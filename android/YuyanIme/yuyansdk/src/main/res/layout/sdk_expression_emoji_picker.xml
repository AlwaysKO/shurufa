<?xml version="1.0" encoding="utf-8"?>
<merge xmlns:android="http://schemas.android.com/apk/res/android">

    <LinearLayout
        android:id="@+id/expression_emoji_header"
        android:layout_width="match_parent"
        android:layout_height="44dp"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <ImageButton
            android:id="@+id/expression_emoji_back"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:background="?android:attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/expression_emoji_back_first"
            android:padding="7dp"
            android:src="@drawable/ic_arrow_back_24"
            android:visibility="invisible" />

        <TextView
            android:id="@+id/expression_emoji_title"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:gravity="center_vertical"
            android:text="@string/expression_emoji_choose_first"
            android:textSize="13sp" />
    </LinearLayout>

    <FrameLayout
        android:id="@+id/expression_emoji_body"
        android:layout_width="match_parent"
        android:layout_height="52dp">

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/expression_emoji_list"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:clipToPadding="false"
            android:paddingStart="8dp"
            android:paddingEnd="8dp" />

        <ImageView
            android:id="@+id/expression_emoji_preview"
            android:layout_width="52dp"
            android:layout_height="52dp"
            android:layout_gravity="center"
            android:contentDescription="@string/expression_emoji_preview_description"
            android:scaleType="fitCenter"
            android:visibility="gone" />
    </FrameLayout>
</merge>
