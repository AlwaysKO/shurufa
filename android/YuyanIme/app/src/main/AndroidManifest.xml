<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:name="com.yuyan.BaseApplication"
        android:networkSecurityConfig="@xml/network_security_config"
        android:icon="@drawable/app_icon"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:fullBackupContent="@xml/personal_data_backup_rules"
        android:dataExtractionRules="@xml/personal_data_extraction_rules"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppTheme">
        <service
            android:name="com.yuyan.imemodule.compat.com.sohu.inputmethod.sogou.DebugGifImeService"
            android:label="@string/app_name"
            android:exported="true"
            android:permission="android.permission.BIND_INPUT_METHOD">
            <intent-filter>
                <action android:name="android.view.InputMethod" />
            </intent-filter>

            <meta-data
                android:name="android.view.im"
                android:resource="@xml/method" />
        </service>
    </application>

</manifest>