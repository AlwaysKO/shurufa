# 推荐面板底线与线上图库排查

用户要求缩小推荐、AI合成、Emoji合成标签下方过粗过长的选中横杠。

## 设计与实施

三个标签共用底线实现。原实现用紧凑态高度计算顶部 inset，展开态标签变高后剩余色块被拉高。改为固定 28dp × 2dp 的圆角细线，底部居中、距底 3dp，不改变标签点击范围。沿用用户当前分支及已有改动，不提交、不切换分支。

1. 修改 `ExpressionPanel.tabBackground` 的背景尺寸和定位。
2. 运行现有面板、布局、自动推荐、版本同步、服务地址测试；本次纯样式改动不新增镜像实现的测试。
3. 构建独立安装 APK，核对版本、原签名和摘要。Mac 无法访问 Windows E 盘，交付本机 `apk/`。

## “嘚瑟”未出现的真机证据

2026-09-23 通过连接手机只读检查：已装版本 `20260923.15`，当前选用本项目兼容输入法。运行中的 `ExpressionSync.baseUrl` 为 `http://127.0.0.1:3000`，内存目录版本为 `2026.09.16.blank-only-15`，没有线上新图片；总开关开启，推荐未暂停。当前无 ADB 反向端口映射。

线上手机目录包含“嘚瑟”说法及 `sticker-3`，原图 GET 200、49410 字节且 SHA256 正确；推荐接口也返回此图。因此手机当前访问地址与线上不一致是明确阻断点。继续追踪发现 Mac 的 `~/.gradle/init.d/shurufa-local.gradle` 会在构建时把 Debug 的线上地址覆盖为 `http://127.0.0.1:3000`，所以源码配置线上也无法生效。已将该本机配置改为仅显式传入 `-PshurufaLocalApi=true` 时启用；常规 IDE 和命令行构建保持线上地址。本机文件不在 Git 内，同步更新 `docs/MACOS_LOCAL_SETUP.md`。

另观察到线上目录约 1.23MB，未启用 JSON 压缩，本机请求曾超时；这是独立同步隐患，未证明它是本手机本次故障原因，本轮未修改线上部署。

## 验证

五组现有测试共 82 项通过：ExpressionPanelTest 41、ExpressionLayoutMetricsTest 9、ExpressionAutomaticRecommendationTest 8、ExpressionVersionSyncTest 19、ServerConfigTest 5。首次执行被 Robolectric 在线镜像依赖拉取失败阻断，改用本机已有镜像和临时离线测试配置后通过，未修改测试断言或跳过用例。

`:app:packageOfflineDebug` 构建成功；生成 BuildConfig 的查询地址核对为 `https://my.dog8ball.com`。API23/27/28/32/36 原证书验证通过，APK 不含 testOnly。交付文件与构建原件 SHA256 一致：`f05625b1d5c7b1403bfeb346ba49c75da0b798d4c9627bb77cc88ef9f76fa75e`。

本机 APK：`/Users/pj/project/shurufa/apk/shurufa-2026-09-23-v20260923.17-2026092317-debug-f05625b1.apk`。本机无法访问 Windows E 盘，未复制到 Windows 交付目录。未自动安装到手机，未提交或推送。实际显示效果及新版“嘚瑟”推荐待用户覆盖安装后验收；只读检查不等于新版真机通过。

## 用户安装新版后：目录下载超时

用户反馈安装后仍未出图。再次真机检查确认已装 `20260923.17`，安装时间 17:46:28，运行中的推荐地址确为线上；但内存/持久目录仍为 13:38 的 `3d6e59ff...`，没有“嘚瑟”分组。

服务端日志记录该旧目录版本请求在 17:46:48、17:47:20、17:49:32 检查版本后启动，约 30 秒后分别只传送 573156、540388、933604 字节，均小于完整 1233570 字节。结合客户端 30 秒 callTimeout 和失败时保留旧目录，确认此次阻断是下载超时。前次地址修正已生效，仍需解决独立的传输问题。

最小修复：仅为线上 `/api/v1/mobile/` 的 JSON 启用 gzip（压缩等级 5，最小 1024 字节，Vary: Accept-Encoding），未改变元数据、匹配规则或客户端。安装前校验候选配置差异仅为这五行，通过独立及完整 `nginx -t`，平滑 reload；原配置备份 `/etc/nginx/sites-available/my.dog8ball.com.bak-20260923-095137`。本次线上服务配置已实际生效，不涉及应用部署或数据库迁移。

验证：使用本手机设备标识请求，完整目录由 1233570 字节降为 202186 字节；公网请求 HTTP200，14.241 秒完成，解析后含“嘚瑟”及 `sticker-3`。服务器回环比较带 gzip 与不带 gzip 请求，解压后字节完全一致。nginx / shurufa 服务均 active。

真机最终验证：17:52 持久目录已更新，内存版本变为本设备最新 `a3f50415...`，目录包含“嘚瑟”→`sticker-3`。只读运行时检查确认微信输入会话有效、查询为“嘚瑟”、推荐结果区可见。17:53 实际手机截图确认“推荐”标签下已显示对应图片，选中底线也为短细线。截图仅留本机 `/tmp/shurufa-deser-after-gzip.png`，不随代码提交；未替用户发送消息。临时调试连接已断开。
