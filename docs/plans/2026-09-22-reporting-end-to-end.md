# 本机聊天上报与OCR端到端复查

用户指出 http://127.0.0.1:5175/chat-capture 停在9月18日。只读排查，不修改服务器/删除手机队列，不把截图入库当作上传成功。OCR正式仍ML Kit；标题带修正已做，王彦兵偶发错字、总体准确率/内存未整体验收。

## 证据
- Vite本机页面默认代理WSL3000。针对手机自身device_id查询本机PostgreSQL：微信121条，最新received_at=2026-09-18T09:53:04.724Z；QQ1条、抖音54条，最新同为9月18日。设备last_seen_at却已到9月22日，设备活跃不能代替聊天上传。
- 手机capture.db的pending_asset=0、pending_message=0、seen_message=2346；LocalInputStore第二层双传队列本机127.0.0.1:3000仍有chat_asset122、chat_messages1953，另旧LAN地址有chat_messages105。两种队列不能混为一谈；报告/批次数量不是最终图片张数。该快照两库quick_check均ok，但为只读拷贝，未冻结手机事务。
- 当前127地址待传聊天报告online_confirmed_at全部非空，说明端侧记录了线上确认，不冒充本轮已查询线上实际数据库。attempted_at均0，尚不能判断具体未推进原因。
- 实际reverse为手机3000→Windows3001，WindowsPowerShell项目中继运行；手机curl访问127与旧LAN地址health分别HTTP200、约10ms/51ms。USB powered=true，当前sys.usb.state含adb。shell连通不等于应用自身门控已放行。
- 当前手机PID31547、版本仍20260922.14 / 2026092214，但实际base.apk SHA已变为ab1e30ae994dbf7ece66b8157da4d66d39b746c3ac34bb76d5417c4356c3234a，不是本轮此前交付7faf9616。此次没有安装或改手机配置，须按实际包继续查。
- 真实数据库快照及设备ID只留ignored artifacts/diagnostics/2026-09-22-reporting，不进入Git/第三方。还没有证明上报修复，后续定位应用内门控/队列读取/HTTP确认哪一环失败，再做最小修复和端到端验收。

## 本轮继续实施（用户要求同时保证线上不静默积压）
验收口径：正常网络时两端队列持续消化并实际到账；断网时保留、恢复自动补传；一端失败不能阻塞另一端。不能承诺服务器不可用时零积压，不删除失败数据制造清空假象。
1. 先增加DEBUG-only无内容分阶段诊断：USB/健康门控、目标忙/退避、注册、队列读取、HTTP状态、确认。日志只含阶段枚举、线上/本机类别、数字/布尔，无聊天正文、标题、设备ID、凭据、完整URL或响应正文。
2. 当前两端健康及手机数据库证据无法确定应用自身卡点，诊断包保持功能不变，保留数据覆盖安装后定位真实失败环节。
3. 根因明确后先补失败测试再作最小修复；覆盖两端独立确认、坏项不饿死新项、断网恢复、成功吞吐与失败退避。
4. 用本机实际数据库新记录和手机队列下降验收本机；线上需独立接收证据，不能仅凭端侧online_confirmed_at或health就宣布到账。优先核对已有授权接口/部署配置，不猜测凭据。
5. OCR后续继续使用原始标题带与人工真值对照，不能因上报修复就宣称标题准确率达标。

## 15:38 诊断结论与补传实施计划
- 用户再次明确线上始终是 https://my.dog8ball.com，不修改地址。
- 原签名诊断包8181938a已保留数据安装，365项测试0失败。PID4408应用内USB/127健康门控均通过，连续3轮chat_asset收到HTTP200且ok=true；线上本轮pending reports=0。旧LAN目标健康失败，不能与当前127目标混为一谈。
- 更换诊断包后开始传输不证明过去停滞根因已修复。已直接复现吞吐问题：15:37:50、15:38:14、15:38:43每轮仅读1个资产，资产优先导致后面的消息批次长时间不出队。
- 采用保留小批量的有界连续补传，不提高单批内存预算、不全量加载、不删除坏项。相比单纯把所有轮询改成1秒，此方案只在有确认进展时继续，空队列和失败不空转。

### 实施步骤
1. EventDeliveryDrainTest.kt：合成大资产跨批连续清理、批数边界、失败保留/恢复、无进展终止、时间预算、取消终止，先在单批stub上观察断言失败。
2. EventDelivery.kt：增加drain，复用单批发送；最多100批且5秒后不启动新批（已开始网络请求仍受15秒call timeout约束）。仅成功落库确认算进展，失败停止此次突发、正常30秒周期再试。每批检查协程取消与现有授权。
3. DataCollector.kt使用drain；目标占用放到协程启动后，supervisorScope与每目标异常隔离，取消仍抛出、finally释放。不得调整个人词库同步行为或服务器配置。
4. collect/capture/service capture全套测试、只读代码审查、原签名构建覆盖安装；核对手机自动队列下降和本机DB新增，不把此前手动转发1组当自动恢复。
5. 线上实际到账与OCR准确率仍分别验收；域名可通或端侧确认记录不代替独立线上证据。不自动commit/push/deploy。

### 用户线上核对与新增回归证据
- 用户本轮回复“能看到今天的记录”，确认线上后台今天确有聊天记录；这是用户页面核对，不等于本轮逐条线上数据库对账或无限期零积压保证。
- drain的6项新测试在单批stub上全部实际断言失败，实现后6/6通过。
- 从原DataCollector调度顺序提取TargetDeliveryTasks复现：启动前取消遗留占用、单目标异常取消另一目标，3项测试中这2项实际断言失败。该证据确认代码风险存在，不证明它就是手机过去停止上报的唯一根因。
- 新增独立失败项公平性验证：事件接口失败/超大事件不能饿死聊天、已失败资产应让位于未尝试消息。保持坏数据与幂等ID，不删除或截断；服务端缺资产消息返回409/ok=false，不能确认丢掉。
- 时间预算细化为每次HTTP请求前检查，不仅新批开始检查；正在执行的同步HTTP仍由现有15秒callTimeout限制，不声称即时取消。

### 审查补充与WSL核对
- 审查发现慢events失败≥5秒仍可反复耗尽预算、饿死聊天。新增模拟时钟回归先实际失败（7项中1项失败），随后按目标逐批轮换events/reports先手，既不取消预算，也避免反向饿死事件。只读复审未发现新增阻断点，完整回归仍以最终构建结果为准。
- 用户指出后台在WSL，询问IP变化。当前hostname -I主IP、Windows netsh旧LAN3000转发目标、运行中USB中继TargetHost均为172.31.94.56；ADB reverse手机3000→Windows3001；手机curl当前127健康成功。当前没有IP错配证据，不将历史停滞一概归因于WSL IP。WSL未来重启IP变化仍可能影响固定TargetHost中继，本轮尚未增加自动重配。
- 旧LAN地址192.168.10.194:3000目前的Windows端口转发也指向同一个WSL3000；手机历史目标仍保留105个聊天消息报告，尚未迁移/删除，不把当前127队列消化当作所有历史地址都已完成。

## 修复包与第一阶段真机验证
- 最终完整collect/capture/service capture回归381项、68套，0失败/错误/跳过，构建成功。独立签名校验API23/27/28/32/36通过、非testOnly。交付`E:\Projects\shurufa-android\apk\shurufa-2026-09-22-v20260922.15-2026092215-debug-284021cb.apk`，SHA256为284021cbcf38d670c51e8577a56b4488fefed6a2e861c63e377a89dc0ecf2e94，源构建/E盘/手机base.apk三方一致，原签名install -r、默认IME未变。版本号与诊断包相同，不以版本号推断包相同。
- 安装前电脑时钟07:58:48Z：当前127目标chat_asset=1、chat_messages=1977；本机后台微信122/抖音54/QQ1。新PID18280日志已出现连续POST_MESSAGES→HTTP200→ACK_RESULT true→STORE_ACK，每个STORE_ACK在手机DB确认后记录。
- 08:00:03Z取到quick_check=ok的空闲期快照：当前127 chat_asset=0、chat_messages=1688；后台07:59:38Z已到微信348/抖音55/QQ1。本次没有电脑手工POST样本，新增为手机自动补传。仍在消化，不宣称已全部清空。
- 第一次在高频写入中直接复制DB的本机副本quick_check失败，已排除该副本作为计数证据；随后空闲期新副本ok。不得把非原子复制失败误称为手机数据库损坏。
- 手机日志墙钟与电脑UTC存在偏差，只在同一时钟内部比较耗时，不跨时钟直接相减。真聊天/DB/日志仅ignored diagnostics；未提交、推送或修改线上配置。

## 16:07 当前聊天通道验收结果与剩余边界
- 电脑08:07:03Z、PID18280、稳定双读副本quick_check=ok：当前127的chat_asset/chat_messages均0；线上目标report_target与event_target均无待传。当前127仍有普通个人选择报告302、位置4正在继续补传，不能说全部类型队列已清空。
- 后台同设备实际微信1939、QQ3、抖音59，合计2001（修复安装前177）；当前聊天积压1977批已消化。最新接收已是9月22日，无需手工POST。数量为服务端去重后的记录，不能要求与报告批数一一相等。
- 新包运行期间有新线上截图/消息入队；手机日志16:09:03.718 POST_ASSET→16:09:04.936 HTTP200/ok→STORE_ACK，随后POST_MESSAGES→16:09:05.271 HTTP200/ok→STORE_ACK；下一轮REPORTS_READY线上为0。与用户确认线上页面今天有记录结合，证明本轮线上真实新数据投递正常，不代表未来断网/服务器故障时永不暂存。
- consent仍true，默认IME仍DebugGifImeService，未清数据、未改线上地址。
- 旧LAN目标仍有105份聊天报告，含129个唯一消息fingerprint；只读用这些指纹+本手机device_id核对当前WSL后台，129个尚未找到。不能忽略这批，也不能用HTTP健康替代到账。需要将已确认同一WSL的旧目标聊天任务迁到当前USB本机通道；尚未修改这些历史目标或删除数据。
- OCR仍正式ML Kit，原始标题带已修但“王彦兵”的总体准确率/内存没有最终验收。截图时机本轮未改，聊天正文不做OCR。
