# scene-rich-07 四秒十二姿势实现计划

**目标：** 没问题、心累、得意、害羞各一张 review-only GIF，延续已批准的 scene-rich-06 节奏，不更改旧批次。

**架构：** 新建独立 renderer、测试、固定路径脚本，复用现有字体和审计函数；12 格母版、20 编码帧、4000 毫秒、姿势 7 停留 900 毫秒，最后四姿势 1380 毫秒回正。全部审计通过后才写成品，不承诺 I/O 错误或中断时原子性。

**技术栈：** TypeScript、Sharp、Vitest。

1. 新建 `server/src/expression/sceneRich07Renderer.test.ts`，覆盖固定四项清单、安全路径、4 列 3 行、顺序时间线、真实首帧 fallback、原审计拒绝静态假动画和无效母版。先运行 `cd server && npx vitest run src/expression/sceneRich07Renderer.test.ts --maxWorkers=1` 记录 RED。
2. 新建 `server/src/expression/sceneRich07Renderer.ts` 最小独立实现，原 06 文件零修改，运行同命令确认 GREEN。
3. 新建 `server/scripts/render-scene-rich-07.ts`，固定 source/output 到 07，拒绝多余参数与母版符号链接越界；输出 GIF / WebP / poses / report / 联系表 / preview，只允许 review-only。
4. 运行 `npm run build` 与 `npm test -- --maxWorkers=1`，记录真实结果。主代理负责生成母版、素材审查、归档和提交。本任务不触碰 catalog/API/query/Android/client 并行变更，也不提交。

## 验证记录
- RED：5 项预期失败，空时间线/网格与未实现验证、渲染；`/tmp/scene07-red.log`。
- GREEN：`cd server && npx vitest run src/expression/sceneRich07Renderer.test.ts --maxWorkers=1`，5/5 通过；`/tmp/scene07-green.log`。
- `npm run build` 通过；`npm test -- --maxWorkers=1`，32 文件、375/375 通过；`/tmp/scene07-build.log`、`/tmp/scene07-full.log`。
- 固定脚本额外参数被拒绝：`cd server && ./node_modules/.bin/tsx scripts/render-scene-rich-07.ts unexpected` 返回非零及“本脚本不接受额外参数”。
- 既有 Fontconfig 缓存版本告警存在，不影响本轮测试。计数反映共享工作区快照，包含其他并行修改，不把它们归因本批。
- 旧 06 renderer/test/script 零 diff；本任务只新建 07 的三文件及此计划，不提交、不归档、不改并行用户文件。
- 此验证使用合成测试母版；真实 4 张母版生成、审计与视觉审查由主代理执行，尚不声称真实素材通过。
