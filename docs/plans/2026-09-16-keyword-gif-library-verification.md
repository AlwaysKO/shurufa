# 全量关键词 GIF 后台入库验收

2026-09-16，依据用户“此前所有的都入库”及后续默认通过授权；不代表逐张观看验收。

## 结果
- 新补录181张关键词成品；原先184张已发布成品按SHA去重，没有重复补录。
- 逐条决策：`assets/expression/approvals/2026-09-16-keyword-gif-import.json`；实际服务清单：`assets/expression/approved-keyword-gifs.json`。
- 15条输出记录排除：7条明确返工/负面反馈、4条被新版本替代的网页旧稿、4条reference-02许可未确认。相同角色的失败旧稿不因用户全量授权复活；reference-01两张有明确用户许可声明，登记licensed而非ai-original。
- 无字合成底图不属于此次关键词补录，未覆盖另一任务的合成库。历史审计报告及原GIF不改，新增批准清单记录新授权依据。
- 后台“个人资产→关键词推荐图（原表情包）”直接展示动图，系统图也有删除按钮。不生成/交付HTML预览。
- 删除按当前所选用户及系统GIF内容SHA生效，后台分组和手机目录/推荐一同移除；版本随之变化。制作原件保留，重导或ID改变不复活；其他用户不受影响，独立个人上传仍由自己的删除按钮管理。

## 验证证据
- 019迁移已在本机PostgreSQL执行。
- 所有补录项均校验实际GIF与旧报告SHA，重新运行原有 `auditPrototypeGif`，未放宽技术门禁。
- `npm run expression:import-keywords` 重跑后清单SHA一致（后续仅完善许可依据文字），没有重复追加。
- 实际本地3000端口：下午好/晚上好/中午好各返回4张；12个GIF URL实际下载成功且SHA与清单一致。
- 真实本地5175后台：通过真实登录进入`/stickers`，搜索晚上好，4张240px图片均解码成功，4个删除按钮可见。使用独立临时浏览器上下文验证，没有删除用户素材。
- 新回归覆盖：来源门禁、未入库成品可见/可推荐、登录要求、路径越界、删除用户隔离、版本变化、ID改变/重导不恢复、保留原件、个人上传不被系统删除误伤、新图使用上报。
- 全量服务测试 `npm test -- --maxWorkers=1`：57文件通过、1文件跳过；566项通过、6项跳过。未放宽测试默认超时。
- 服务端TypeScript构建及前端vue-tsc/Vite构建通过；前端仍有既有大包体警告。
- 独立只读审查发现的使用上报旧目录、系统删除误伤同SHA个人图、路径允许子根过宽、dry-run覆盖正式报告四项已修正；对应新增失败测试先复现，再通过。

## 后续入口与边界
每批关键词成品后执行 `cd server && npm run expression:import-keywords`，核验后台和推荐实际可读后交付。导入仅扫描各批最终output/report，不扫描rejected、样例与失败中间文件；新增不同输出结构须显式补接，不假装自动覆盖未知路径。

本次未推送远端、未重打APK。工作区存在其他会话未提交的后台/合成/Android改动，本轮保留它们，没有为提交本任务打包其他改动。
