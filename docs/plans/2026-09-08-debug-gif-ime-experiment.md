# 调试版 GIF 服务组件实验

用户已明确同意可回滚实验。仅改变微信看到的输入法 Service 组件标识，不修改 applicationId、签名、素材或发送协议；保留旧服务。实验标识使用我们自己的命名空间前缀，组件名包含接收端匹配串，界面明确显示雨燕实验入口，不称作搜狗。只放 app/src/debug，发布版不可包含。

步骤：先增加源集隔离/清晰标签/原服务保留测试并确认红；将现有 ImeService 标记可继承，新增无业务逻辑的 debug 子类及 manifest 注册；运行测试并构建，检查 debug/release 合并清单；覆盖安装不清数据，由用户选择实验入口后在文件传输助手同素材验证。不得自动代发消息。不以命中字符串或安装成功认定 GIF 实机成功。

回滚：选择原雨燕输入法即可；原服务仍在。保留应用数据与原 60f 字号，不改其他用户文件。
