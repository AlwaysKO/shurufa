# scene-rich-05 九姿势 GIF 实现计划

> **For Claude：** 必需子技能：使用 superpowers:executing-plans 来逐任务实现此计划。

**目标：** 按已认可的三秒九姿势节奏制作你猜、听我解释、嫌弃、困了四款评审 GIF。

**架构：** 复用 seriousV2Renderer 的九格裁切、字体、16 帧正向时间线和原审计，仅扩展固定的 05 白名单入口。03 v2 和 04 公开接口不变；不改正式 catalog、API、Android、归档。

**技术栈：** TypeScript、Sharp、Vitest。

### 任务 1：受限入口 TDD
- 修改 `server/src/expression/seriousV2Renderer.test.ts`，增加 05 四项清单、跨批/非法字段、九姿势真实 GIF 与静态伪动画拒绝测试。
- 运行 `cd server && npx vitest run src/expression/seriousV2Renderer.test.ts --maxWorkers=1`，先确认新入口缺失失败。
- 最小修改 `server/src/expression/seriousV2Renderer.ts`，添加固定四项白名单和包装入口；编码参数、时间线、审计不变。
- 重跑同一命令至全绿。

### 任务 2：固定评审脚本
- 新建 `server/scripts/render-scene-rich-05.ts`，沿用 04 固定路径、清单验证和源真实路径边界，全部审计通过才写成品。
- 输出 `artifacts/expression-batches/scene-rich-05/` 的 GIF、首帧 WebP、report、contact-sheet 和 preview；九姿势保存源目录。
- 仅 review-only，不承诺磁盘错误原子发布。主代理生成母版与清单、运行真实成品和视觉审查。

### 任务 3：验证交接
- `cd server && npm run build && npm test -- --maxWorkers=1`。
- `git diff --check`，记录真实结果和并行工作区变化。
- 交规格与质量独立审查；本子任务不提交，不修改用户并行文件。

## 代码实现验证记录
- TDD RED：新入口缺失，2 项失败、9 项通过；日志 `/tmp/scene05-red.log`。
- TDD GREEN：`seriousV2Renderer.test.ts` 11/11，通过既有 03 v2、04 及新增 05 测试。
- `npm run build` 通过；`npm test -- --maxWorkers=1` 30 个文件、357/357 通过（当前并行工作区快照）。
- Fontconfig 有既有缓存版本告警，无测试失败；未修改字体环境。
- 新增代码仅 05 白名单、包装函数与固定脚本；未修改编码、时间线或门禁。
- 工作区已有 Android、client 和 server catalog/API 测试等其他并行变更，未触碰；此计数包含当时工作区，不代表这些变更由本任务产生。
- 真实母版渲染、机器审计及视觉审核由主代理在图像落盘后执行，本记录不预先声称成品通过。
