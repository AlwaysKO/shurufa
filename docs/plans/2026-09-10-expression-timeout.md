# 图片处理超时恢复与阶段定位

用户确认准备提示超过30秒仍无结果；未连接真机，不能断言下载/渲染/微信交付哪个阶段卡住。证据：网络callTimeout30秒只限制单个下载，ExpressionFlowController整条互斥任务无deadline，prepare挂起即可无限占用并阻挡重试。用永不完成的prepare/send夹具复现该控制流缺陷。

1. Flow测试覆盖准备挂起、发送挂起、超时后重试与外部取消，先失败。
2. 在互斥任务内用总超时保护；区分超时和外部取消，超时复位并返回明确阶段信息；禁止超时后走相册fallback/继续发送。
3. UI分开准备、交付、保存状态，保留现有job身份校验和finally清理。日志不记用户文字。
4. 验证Flow/UI定向测试及APK构建，恢复SDK配置。不承诺能打断主线程阻塞的Binder或不响应取消的CPU编码；这些仍需设备日志定位。

验证：两个挂起测试在修改前失败；修改后33项定向测试通过，包括真实InputView准备超时后隐藏遮罩/旧任务释放不发送/点击重试及准备失败恢复，13项工具回归通过。APK构建成功，git diff --check通过。独立审查无阻断问题。已恢复local.properties；产物 yuyanIme_2026091014_debug.apk，未安装。

补充观察：内置目录60个合成模板，前12个原图存在且SHA匹配；后48个原图不在APK源assets中，在server/.runtime/expression-assets存在，点击需要缓存或网络回退。未知用户点击哪张，不能将此直接归为本次故障原因。仍待设备侧阶段提示/键盘是否响应/点击模板信息，不宣称AI合成实际发送已恢复。
