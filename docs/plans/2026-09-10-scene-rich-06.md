# scene-rich-06 四秒十二姿势实现计划

目标：不行、摸鱼、委屈、期待各一张 review-only GIF。四列三行十二独立姿势；20 编码帧、4000 毫秒，开场 400 毫秒，第八姿势 900 毫秒，其后四姿势共 1380 毫秒回正。按顺序播放，不倒放、不淡化，不把停顿重复帧描述为独立姿势。

1. TDD：新建独立 renderer 测试，覆盖固定清单、4:3 舍入网格、正向时间线、真实首帧 WebP、原审计拒绝静态假动画和无效母版。
2. 新建独立 renderer 和固定路径脚本，保留旧批次代码不变。字体与编码方案沿用现有实现，无新增运行依赖。
3. 全批审计通过后才写 GIF、缩略图、report、联系表、预览及源 poses；不承诺磁盘错误或进程中断的原子性。
4. 跑测试、构建、单 worker 全测，交主代理制作母版、视觉审查、归档和提交。不更改并行 catalog/API/Android/client 文件。

## 验证记录
- TDD RED：五项测试因缺失实现失败（空时间线/网格、未实现验证和渲染），日志 `/tmp/scene06-red.log`。
- TDD GREEN：`cd server && npx vitest run src/expression/sceneRich06Renderer.test.ts --maxWorkers=1`，5/5 通过。
- `npm run build` 通过；`npm test -- --maxWorkers=1`，31 文件、362/362 通过；日志 `/tmp/scene06-full.log`。
- 既有 Fontconfig 缓存版本告警仍存在，无测试失败。`git diff --check` 通过。
- 本次只新建 renderer、测试、固定脚本和本计划；旧批次、并行 catalog/API/Android/client 均未触碰。全测计数反映当前共享工作区快照，不代表并行变更出自本任务。
- 真实母版和成品视觉审查由主代理执行，此记录不预先声称真实四张素材通过。
