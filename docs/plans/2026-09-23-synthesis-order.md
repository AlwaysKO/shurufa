# AI 合成底图排序

目标：后台底图可拖拽、前后移动并保存；新上传默认置顶，手机同步后展示同样顺序。

设计：按当前用户保存完整可见底图ID顺序（系统与个人均可排列，不改变系统文件只读权限）。新增个人底图优先于已有保存序列，已有项相对顺序不变；重复上传不置顶，删除不复活。独立030迁移，后台和手机目录复用同一排序函数，顺序纳入目录版本。Android新增可选排序元数据，旧目录保留相关性排序。

实施：前端独立并行实现，主代理负责服务端和手机兼容；当前main原地开发，保护上一轮新图下载修复，不提交推送。

验证：先写后端和手机回归测试再实现，覆盖新上传/重复上传/刷新/用户隔离/非法ID/版本变化；前端覆盖拖拽/保存失败/取消。执行本地SQL迁移（仅030，先核对本地地址）；前后端构建、真实隔离PG与浏览器检查；打包原签名APK。线上不自动迁移或部署，不自动安装手机。

## 本地验证记录

- API/目录/公共图库/API客户端六组测试共100项通过；真实隔离PostgreSQL底图库25项通过。
- Android目录、自动推荐、后台同步、版本同步共56项通过；目录排序经过缓存落盘重载验证。
- 真实PostgreSQL设备排序清理独立用例通过，另一设备数据保留；公共图库迁移/清理两项通过，补齐030夹具。
- 本地开发库已执行030，核验新增表/字段；真实后台列表与手机目录均200，15张底图顺序一致。未修改线上。
- 浏览器覆盖真实拖拽、前后移动、取消、保存失败重试、刷新持久化、新上传置顶、编辑替换批量删除和窄屏；复核新增上传成功但刷新失败的边界断言，先红后绿。
- 前后端构建通过。Android包原签名API23/27/28/32/36验证通过，未设置testOnly。
- 额外设备控制全量旧用例有两项失败：公共sticker删除断言与现有保留规则冲突，原HEAD测试复现；词库快照400单独重跑原HEAD用例通过，尚未定位全量顺序影响。本轮不改变这些无关功能，不宣称全仓测试通过。

交付包：`apk/shurufa-2026-09-23-v20260923.23-2026092323-debug-10a1f7aa.apk`。
SHA256：`10a1f7aadf00e685d40719f9fe51a7ed2a1d8c25c4f3cb4690cf9ce62c221579`。
本机为macOS，无法访问既定Windows E盘目录，未复制到E盘；未自动安装、提交或推送。手机实际展示仍待升级和线上部署后的真机验收。

## 推送冲突合并验证

用户重试推送时发现 main rebase 停在 a741ee1。保留远端原尺寸、多格式、二进制上传、上传进度与局部更新，合入底图排序及新图下载修复。相应浏览器夹具改用二进制协议，覆盖新上传不刷新全库、清除旧排序草稿和手动刷新失败后仍置顶。前后端152项、两组浏览器验证、Android目录和同步56项及前后端构建通过；未强制覆盖远端历史。
