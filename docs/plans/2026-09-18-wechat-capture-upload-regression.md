# 微信截图停止上报排查

用户：最后上报2026-09-18 17:47:47，安装最新原签名debug包；手机不能连接，修复验证后打包，不部署/推送。

## 已核对
- 17版988f16db与18版75c7425a的采集service反编译除来源注释一致；5个相关类的反编译也一致，说明不能归因为刚才签名设置改动。相关DEX进一步核对中。
- 最近采集改动在393c80f引入空树截图OCR聊天页门控；标题缺失/不识别时阻止保存，旧版待确认截图不受该门控。不能移除非聊天页保护。
- 发现空树截图固定先裁顶部80物理像素，而API34+输入已经是应用窗口截图；裁剪可切掉标题/导航，使新增OCR门控拒收真实聊天。以窗口标题在前80px的夹具复现，修复仅保留窗口自身顶部，键盘底部排除不变，不扩大截图到其他应用。
- 之前数据核对曾暂停应用；尚需用户确认升级后无障碍服务连接与采集授权。手机无日志，不能宣称已唯一定位现场原因，也不能把模拟回归当真机恢复证明。

## 验证计划
- 先红：窗口顶部完整保留、聊天标题可识别、列表仍拒绝、键盘范围排除；后绿并回归现有采集/去重/上报队列测试。
- 保留原签名、无testOnly，新包交付固定E盘目录。用户安装后确认无障碍/采集开关并产生真实新消息，检查后台截图时间。
