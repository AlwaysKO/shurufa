# 长组合前缀候选消失排查与最小修复计划

**目标：** 修复已安装 .15 版继续输入时可信前缀被大量单字挤到末尾的问题；不擅自放开未知整句，继续核实用户所述整栏空白。

**架构：** 保留现有整词、末字前缀读音约束和个人学习优先级。仅在所有完整候选之后，合并旧词库/公开词库的可信原生前缀，保留原生相对顺序与选择索引。

**技术栈：** Kotlin / Robolectric / 安装 APK 原生 JNI 隔离 app_process。

## 已确认的证据与边界

Windows ADB 已找到设备，安装版本 20260914.15。使用安装 APK 的代码、原生库和资产，新建 /data/local/tmp 隔离词库，不读取用户历史、不操作聊天输入框、不安装。

wo bu zai pang huang 全码 9628924726448264，末字 h 为 962892472644；共16个逐键前缀。全码原生首项“我不再彷徨”，随后“我不在/我不再”；后处理移除未知整句并把公开原词前缀放到95个单字之后。公开源码1.11.2对照与定制JNI的候选数量不同，不能替代设备证据。

这16个隔离输入没有整栏变空，因此用户是否点击过前缀、实际残码与UI空栏仍需核实。不得宣称本轮已证明彻底修复全部空栏问题，也不以样例句白名单放行全句。

## 实施步骤

1. 保存已安装APK的16个真实原生数组为测试fixture，增加 `T9LongCompositionTest`，断言从“我不再”全码起，继续逐键输入仍可在前8项选择该前缀，索引一致。先跑出红测（全码前缀实际在95位）。
2. 修改 `OfflineT9Candidates.kt`：完整候选分级不变；原生前缀合并后按nativeIndex排序，而不是把公开前缀全部放到单字后面。不修改词库、.so、拼音边界、学习权重和UI。
3. 运行新测试及 `PublicPhrase*Test`、`T9*Test`、`Personal*Test`、`OfflinePersonalCandidatesTest`、`KeyRecordStackTest`、`CandidateSelectionTest`、迁移/提交测试。包括305录制数组和近期用户例子，记录实际计数。
4. 在同一真实设备录制数据对比首屏与选择索引；验证全码和末字首字母的“我不再”分段后能继续选择“彷徨”，对退格再输入做原生测试。
5. 构建隔离debug APK，核验包内容；只读复审。保留原有未提交工作，不自动安装或提交。

## 本轮执行结果

只修改候选前缀排序的已确认问题，精确改动见诊断目录 `scope.patch`。先红：962892472 的“我不再”在索引96；后绿：17套件96项，0失败/错误/跳过。305组与上一批公开索引接入结果对比已有正确首选退化0、召回丢失0；107条原先首选未命中的输入首项发生变化，不把它们全部计为语义改善。

手机隔离46步前进/退格/再输入，修前修后原生数组完全相同，修后从wo bu zai起前缀始终前8。新APK独立冷库中，末h/完整huang两码排序后的“我不再”位置1映射原生2，选中后余码“彷徨”位置1，原生commit完整句子。自动测试另验证分段选择3次的学习，在数据库重开后两码个人整句首选；不是实际宿主/触摸学习测试。

`.16` debug APK已打包、签名验证有效，未安装手机、未提交。真实整栏空白仍未复现，未宣称彻底解决。本轮尝试装配完整RimeEngine的额外诊断进程被Killed、无有效结果；不算通过。事实和重跑方式见 `artifacts/diagnostics/2026-09-14-t9-long-composition/README.md`。

## 后续截图纠正（2026-09-14）

用户已澄清：不是候选数组为空，而是选字前只看到“我不在”对应的前半拼音，选字后尾部才出现。上文的前缀排序处理及“整栏空白未复现”是当时问题理解，不能代表已经解决这个显示症状。后续根因和修复见 `2026-09-14-t9-full-preedit.md` 与 `artifacts/diagnostics/2026-09-14-t9-full-preedit/README.md`；旧证据保留，不抹掉历史。
