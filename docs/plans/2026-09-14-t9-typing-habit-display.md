# 用户输入习惯与残码显示修复

来源：2026-09-14 当前用户反馈“明天去爬山”显示 mingtian78727；明确通常前面各字全拼，末字首字母或全拼。根据该拼音与残码推定本次总码6464842678727，另测完整6464842678727426；不是已取得手机按键日志。

## 最小改动范围

1. 保留按键/读音对齐 `T9Spelling.preedit`、候选资格、学习键、原生候选index，不用显示修复改变这些规则。
2. 添加独立显示函数：有完整读音时显示实际已输入拼音；只有前缀读音时保留已知拼音并用“〔待选拼音〕”显式提示未解析部分，不能猜读音、显示数字或清除按键。空读音显示同一提示，空输入为空。
3. 提示只放在候选栏上方显示文本，不用于commit，也不参与词语学习。已向用户询问临时提示偏好；当前使用明确可见的待选提示，不直接隐藏未解析状态。后续偏好答复可仅调整显示。
4. 回归测试先证明旧显示回退会泄漏数字；再改显示调用。完整前缀仍可分段选择，内部简拼仍不放开。

## 召回对照

来自公开语料119个完整读音，各生成末字首字母和全拼，共238行，见 artifacts/diagnostics/2026-09-14-t9-typing-habit/。该套件含历史语料，不宣称新留出。

基线首选：全拼99/119、首字母39/119。上一轮末端补全实验：全拼98/119、首字母73/119。“明天去爬山”短码两臂均未在前100找到；实验完整码也退化。这个已知红项继续阻止原生实验发布，与本次显示修复分开验收。

不安装APK、不替换原生库或词库。不得宣称显示修复完成等于目标候选已正确或全输入已验收。

## 实现补充与验证

进一步定位到显示缓存同时供 `composingStrForCommit` 使用，因此最终只改 `DecodingInfo.composingStrForDisplay`，未改RimeEngine缓存。软/硬键盘Enter在残码未解析时不提交也不reset，提示永远只用于显示。

显示和门禁测试先红再绿；隔离12套件86项，85通过、1既有缺陷跳过、0失败。独立审查无阻断性代码问题；软/硬键盘回车的实际UI端到端和真机仍未验收，不安装本轮源码包。候选召回仍是独立未解决项。

### 后续用户否定与取代

用户随后安装反馈“待选拼音很奇怪”。按该反馈取消提示文字，只显示原有已对齐拼音，不猜测余码读音；仍不丢内部按键，保留回车保护。空显示不等于组合结束，异步推荐入口改读Kernel.isComposing。此后续决定取代上文的显示提示设计，不追溯称其已获用户认可。新候选过滤方案的策略冲突另见2026-09-14-t9-public-phrase-index.md。

### 2026-09-14 完整输入显示纠正

用户进一步用“我不再彷徨”前后截图明确要求：未选前缀候选时就要看到后半输入。上面的“只显示原有已对齐拼音”会把仍存在的尾部按键隐藏，已由 `2026-09-14-t9-full-preedit.md` 的独立完整拼音显示方案取代。优先使用按码对齐读音；真正未知的余码以键帽字母组保留，不再删除，亦不恢复数字/“待选拼音”文案。原内部组合、回车保护和候选资格仍保持不变。本条保留历史取代关系，不把过去方案记作当前要求。
