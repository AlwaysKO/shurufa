# 已确认梗形象续批计划

目标：按用户确认的形象与动态规格，新增真有你的（熊猫）、学到了（蘑菇）、拒绝（熊猫）、搞不懂（蘑菇）4 张；旧图不改。

1. 内置图像工具逐张生成 4×3 十二姿势母版，保存提示词、参考及 SHA。沿用已确认黑白人脸拼贴形象，各词表演不同。
2. TDD 扩展现有 renderer 固定允许列表为独立第二批；原入口默认仍只渲染第一批，新 CLI 仅显式 batch02 选择，禁止任意路径/来源/发布标记。
3. 用现有 20 帧/4 秒时间线与完整原质量门禁渲染，失败修母版，不降低标准。生成隔离报告、动态预览，抽帧验图。
4. 定向测试、构建和 diff 检查。新图不自动入 APK/API，不修改其他并行任务；来源保持 user-provided-reference。第一批用户已确认动态，单独记录确认，不改变授权状态。

## 完成记录

- 4 张成品全部通过原始审计，均 240×240、20 帧、4000ms；字节分别 104144、86290、101657、84675。已抽查 0/5/10/15/19 帧。
- 学到了修正第 8 姿势左右手切换；拒绝旧母版循环不合格，定向修复两次仍不合格后重生成更明显的闭嘴/撇嘴/停手动作，最终通过。失败母版与完整提示词保留，未改变门禁。
- TDD 新增四词测试：先 1 失败/6 通过，实施后 7/7 通过；最终相关两测试文件 20/20 通过。服务端构建通过；非法 CLI 参数拒绝；git diff --check 通过。4 个母版与 4 个成品 SHA 一致。
- 产物及提示词保存在 artifacts/expression-character-trials/reference-02-animated；本轮未重渲染第一批、未修改旧图、未加入 APK/API。
