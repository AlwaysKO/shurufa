# 晚上好／中午好网页新稿计划

用户连续“继续”，本轮明确宣布在新稿沿用已允许的构图整理方法，不改旧图，不扩散到历史素材自动重绘。

1. 按提示词包K006/K007各四种风格制作共8份母图；晚上好为清醒柔和问候，中午好加入午餐邀请，避免同一动作只换字幕。
2. 每图独立网页对话，下载/解码/SHA/来源记录后删除并复查；失败暂停下一张。原图只留本地。
3. 逐张看动作、肢体、道具和源图基线；有需要的新稿使用同一缩放比例及显式脚底基线，补白不补画，失败留档不凑数。
4. 最小追加固定web02清单和测试，TDD先验证未知ID失败再接入；固定渲染脚本复用现有gridFraming/renderer，来源SHA与对话删除状态门禁保留。回归、构建、真实GIF规格/fallback/静态抽帧检查及独立审查。
5. 所有结果仍待用户动态审核、不接入APK/API；只提交候选GIF与必要代码和文本，不提交源图或失败GIF，不push。
