# 配套公开整词校验索引与“你赢了”回归

用户本轮拒绝“待选拼音”占位文案，并反馈“你赢了”显示nixing残码。按既定习惯测6494645/64946453。ying/xing同码，允许用户选择并学习，不能强制某个文字特例永远第一。

根因：当前130860条配套词表不含“你赢了”，过滤层trusted拒绝未收录整词；已锁定rime-ice ext有原词条，源码引擎两码分别第4/第2召回。

最小方案：
- 不替换原生.so/table，不改变解码、频率、拼写限制。
- 将同批锁定公开五表全部符合中文词条范围的文本编为排序、可内存映射的精确成员索引；不是Bloom过滤，不接受哈希碰撞，不把整个百万词表反序列化进Kotlin集合。
- trusted在既有词表外检查该索引，候选仍须经过原生读音对齐；首屏/后页共用同一判断。仅增加公开原词收录依据，不允许任意字串组合。
- 随资产保存来源锁、上游许可证与来源头；不改变本仓库其他代码许可。不在这一步做公开发布。
- 显示去掉占位文案，仅保留原有已对齐拼音，未知数字仍在内部；保留上轮回车防残码提交保护。空显示不是空引擎状态。

验证：索引构建与精确查询先红后绿；缺失/损坏索引安全回退旧词表；原生你赢了首屏及后页不误删、同码你醒了保留、选择3次后原码和全码优先；不匹配读音仍拒绝。回归我饿了/真的吗/玩漂流及既有怪串。去掉marker的显示测试与空输入/英文不变。记录资产体积、构建与测试结果；不安装手机，不声称“明天去爬山”整句已修好。

## 实验结果与当前接入状态

索引实际1855331条、29820648字节。精确索引单测通过；应用层模拟原生候选测试中“你赢了”两码首屏/后页保留，选择3次后两码第一；原先跳过的我饿了/真的吗/玩漂流测试也通过。

但广泛测试发现旧规则冲突：公开原表包含夜魔（ye mo，1111），索引会让它低位出现。旧测试要求不出现，而用户曾表达不想要该词。没有修改这条测试来假装无回归，已询问用户“低位可出现/完全不出现”的偏好。

**在回复前，公开索引接入从生产路径撤离**，保留 artifacts/diagnostics/2026-09-14-t9-ni-ying-le/prototype/、待接入patch、来源/许可和测试结果；生成的29.82MB索引仅在/tmp/shurufa-ni-ying-le/，正式assets与noCompress配置均未保留。本轮当前源码只保留去掉占位提示，以及异步推荐改用真实组合状态的显示边界修复。

因此不能宣称当前可安装源码已修好“你赢了”候选。也没有安装手机或公开发布。索引格式/偏移损坏可回退，但运行时不承诺检测所有正文位翻转；构建时源锁逐文件校验。

### 用户确认：低频真实词可以低位出现（2026-09-14）

来源：用户当前明确答复“夜魔这类真实低频词，允许它出现在靠后的位置”。此前待确认的取舍已明确：允许真实低频词低位出现，不得因此抢走常用词或用户已学习词的优先位置。后续可按此约束调整“夜魔必须完全消失”的旧测试；这不是所有公开词条质量已验收，也不是批准安装或发布APK。

用户同时询问是否仍会出现不真实词语。当前不能保证零乱词：已有反例测试/精确收录校验不等于全语义覆盖，公开原表本身也可能包含噪声；个人学习和真实词的语境适配另有边界。该确认尚不代表待接入索引已实际接入源码或手机。

### 用户要求继续执行后：已接入并完成打包验证

按已确认的“真实低频词允许低位出现”恢复公开索引生产接入；夜魔测试改为保留低位且怎么第一，充电吧低位且充电宝第一。之前暂撤离状态不再是当前状态。

- 生产资产1855331条、29820648字节，公开来源锁与许可证随包保存；app/sdk为t9idx配置不压缩，实际APK ZIP方法0、索引哈希与来源构建清单一致。
- 15套件92项通过，0失败/0跳过；原来我饿了/真的吗/玩漂流的跳过测试已解除并通过。
- 305个录制原生候选输入在真实App过滤层重放：未丢掉原有正确首选，列出的已知乱串未进入首屏。287正向语料首选57→138；12反馈编码9→12；你赢了两个码从被删到可选，学3次关闭DB再开后两码第一。
- 该统计是录制开源C API候选+App过滤，不是手机闭源JNI/触摸验收。未安装手机，未改原生libyuyanime.so（APK哈希与前批基线一致），不保证所有自然短句首次召回。
- debug APK构建和签名校验通过；证据与路径见 artifacts/diagnostics/2026-09-14-t9-public-integration/。未提交、未公开发布。
