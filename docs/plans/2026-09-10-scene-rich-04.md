# scene-rich-04 九姿势 GIF 实现计划

> **For Claude：** 必需子技能：使用 superpowers:executing-plans 来逐任务实现此计划。

**目标：** 按用户认可三秒节奏制作等一下、别闹、吃瓜、憋笑四个评审 GIF。

**架构：** 复用 seriousV2Renderer 九姿势裁切、16帧顺序时间线与原审计；保留旧包装接口。新批次使用固定四项白名单和固定源/输出路径，不改正式 catalog、API、Android。

**技术栈：** TypeScript、Sharp、Vitest。

### 任务 1：受限批次入口（TDD）
- 修改 `server/src/expression/seriousV2Renderer.test.ts`：先测试四项清单、非法/重复/跨批/越界路径，以及新词九姿势实际 GIF、首帧缩略图。
- 运行 `cd server && npx vitest run src/expression/seriousV2Renderer.test.ts --maxWorkers=1`，预期新函数缺失导致失败。
- 修改 `server/src/expression/seriousV2Renderer.ts`：提取仅受限 id/文字/位置的九姿势渲染入口，原包装保持行为；不改变编码或质量门禁。
- 重跑测试，预期全部通过。

### 任务 2：评审脚本
- 新建 `server/scripts/render-scene-rich-04.ts`：验证固定清单、真实路径限制源目录；全部渲染审计后写 GIF、WebP、九姿势、报告、联系表与动态预览。
- 明确仅机器审计、人审待定，不自动发布；磁盘错误不承诺原子写入。
- 主代理负责源图片、清单与真实成品运行/视觉审查。

### 任务 3：验证与交接
- `cd server && npm run build && npm test -- --maxWorkers=1`。
- `git diff --check`。
- 实现后交独立规格、质量审查；本子任务不提交，不修改其他工作区变更。

## 实际执行补记

别闹的戳脸方案因循环接缝失败，最终改为极简鸟推开逗弄羽毛；失败母版保存在源rejected/。最终四张4/4通过，详细验收数据与可见局限见本批production-notes.md。门禁未改。
