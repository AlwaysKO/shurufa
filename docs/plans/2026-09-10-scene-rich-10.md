# scene-rich-10 连续生产计划

用户已授权继续按已确认风格生产，无需逐批等待确认。只新增评审素材，不改正式 catalog/API/Android 和用户并行修改。

## 创意
- 下班了 offwork：极简小角色在桌前慢慢合上笔记本，得意抱臂，缓慢打开回初始；克制的解脱感，不靠冲出画面。
- 忙着呢 busy：极简角色一手忙着桌上文件，一手举掌示意稍候，死鱼眼看镜头后回到工作。
- 满足 satisfied：原创写实胖橘猫靠在沙发，缓缓眯眼，腮帮放松，睁眼回正；饱足的幸福。
- 疑惑 puzzled：原创写实棕白狗听不明白，眉眼犀利，头微歪后回正；不重复惊恐大眼。

两张极简+两张真实动物，避免统一可爱模板；所有字符原创，不复制参考IP。4秒、12格来源姿势、20编码帧，重点900ms，最后四姿势1380ms回正。回正须画出逐步变化，不以延迟或重复帧冒充平滑。

## 实现与验证
1. 子代理 TDD 新增 sceneRich10Renderer.ts/.test.ts 与 render-scene-rich-10.ts，基于09独立最小实现，不重构旧版。固定id scene-rich-10-offwork/busy/satisfied/puzzled，中文如上；前两张top-left，后两张bottom-left。保留固定路径、额外参数拒绝、真实符号链接越界测试、旧质量门禁和首帧fallback。
2. 主代理用内置 imagegen 生成四张4×3 PNG母版；保存提示词、源路径、SHA、失败稿。先视觉看图，必要时修图。
3. 运行 npx tsx scripts/render-scene-rich-10.ts，输出GIF/WebP/poses/report/联系表/preview。total=4 pass=4 fail=0；review-only，humanReview=pending。
4. 依次规格审查、质量审查；实际母版与GIF抽帧检查贴词、字幕、连续性。发现跳变修图再审，不宣称机器通过等于用户验收。
5. npm test -- --maxWorkers=1 与 npm run build；归档server/images四词及索引，全归档SHA验证；git diff --check后仅提交本批文件。未改Android，不运行APK/真机，不声明正式接入。

## Pipeline 实施验证
- 仅新增 10 的 renderer、测试与 CLI；旧 09 与审计实现未修改。
- TDD：先以 10 的固定四词规格调用旧 09，实现缺失造成 3 项预期失败、3 项既有时序/边界测试通过；新增 10 实现后 6/6 通过。日志 `/tmp/scene10-red.log`、`/tmp/scene10-green.log`。
- `npm run build` 通过；`npm test -- --maxWorkers=1` 在当前共享工作区 35 文件、392/392 通过（含用户并行修改，不将全量结果归因于本批）。日志 `/tmp/scene10-build.log`、`/tmp/scene10-full.log`。
- CLI 额外参数拒绝验证退出非 0；真实文件系统符号链接越界测试通过。
- Fontconfig 存在既有缓存版本警告，不是无警告执行。此处仅管线测试，不代表实际新母版审计或人工视觉验收。
