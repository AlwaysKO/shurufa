# 九宫格只允许末音节简拼与候选箭头微调

证据：实际词库 284269 的最高分候选长条形由 c + tiao + x 得到，两处音节被扩写；不高兴未在补充词库中。仅限制词长不能解决该问题。

目标：数字码本地词库只允许末音节简拼，前面音节须完整；部分拼写结果放到原生候选后，完整拼写保留词库排序。已有字母拼音匹配不变。拒绝的词也从历史与原生分页移除，不硬编码具体词语、不添加特例词库。

步骤：
1. 回归测试验证 284269 不召回长条形、649439 保留你这样混拼、学习记录不重新注入、原生首选与索引保留。
2. T9Lexicon 匹配时收集被拒绝的内部简拼词；OfflineT9Candidates 构造包含排除集的 CandidateSelection；分页复用排除集。
3. 候选箭头图标参考尺寸 23 改为 27，通过缩小内边距生效；工具栏 ICON_SCALE 不变。
4. 运行相关词库/排名/候选 UI 测试和 Debug 构建，独立审查，恢复本地 SDK 配置。不修改其他功能，不自动提交或安装。

验证：修改前相关测试出现 4 项预期失败；修改后 44 项定向单测通过，12 项工具回归通过，assembleOfflineDebug 成功，git diff --check 通过。独立审查无阻断问题，已恢复本地 SDK 配置。未连接真机验证；原生/历史词的内部简拼过滤覆盖本地词库识别出的排除项，不承诺过滤未收录词的全部拼写情况。
