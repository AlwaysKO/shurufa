# 九宫格 Rime 语言模型隔离评估计划

> 后续实现按 superpowers:executing-plans 逐项执行；当前仅执行试验撤离及兼容性调查，不提交、不安装。

**目标：** 改善自然短句、常用词与个人学习的整体体验，不再靠词性/字数补丁扩展放行。

**架构：** 保留按键、读音匹配、原生候选索引和成功上屏后的个人学习。优先调查现有 Rime 的模型接入能力；公开上游源码/模型只在隔离环境评估，结果不等于手机集成完成。

**技术栈：** Kotlin/JVM、Android Rime 预编译库；上游 librime 与 librime-octagram。

## 用户确认与范围

2026-09-14 用户在“暂停词性/字数补丁、优先核对现有 Rime 引擎与模型、隔离对照”的建议后要求“继续，目标是让输入法好用”。不视为批准三字放行方案，不视为授权更换原生库、安装或提交全部代码。

## 任务 1：撤离失败试验

- 在 `OfflinePersonalCandidatesTest.kt` 添加“本饿”不应进入候选的真实词库反例，先确认失败。
- 仅撤销 `OfflineT9Candidates.kt` 本轮 sentenceLexicon 加载、trusted 放行、newSentences 排序改动，保留前轮迁移/学习改动。
- 将失败的 NativeSentenceLexicon 实现、构建工具与资产移至非构建诊断归档，避免 Studio 重新打包它。
- 正常短句期望测试明确标记未解决，不删除失败事实，不以回退通过测试声称“我饿了”已修好。
- 重跑 completion/collect/T9/KeyRecordStack/提交学习相关测试。

## 任务 2：核实引擎与模型

- 记录二进制哈希、版本线索、内嵌九宫格方案、模型资产和插件符号证据。
- 阅读公开上游 Octagram 实现、数据格式、模型配置及授权；固定来源版本。
- 区分“有 Grammar 接口”和“已加载模型”；未获得运行证据不能宣称启用成功。
- 现有预编译库授权以实际取得版本与项目授权核实，不把今天的上游说明自动追溯套用到旧包，也不绕过技术限制。

## 任务 3：评估与下一阶段门禁

- 原始候选：沿用 .10 快照、291 组正常/反例/留出及 4680 短码数据；记录目标召回、名次和新增坏词，不能仅计算正常词数量。
- 候选重评分实验不能代替在解码搜索中启用模型的结果；未召回目标尤其不能仅靠重排修复。
- 另验证个人学习 3 次及重启、完整码/末字前缀、退格、分段、后页原索引；测延迟/内存，离线、不读取真实私人输入。
- 若需要重建原生引擎，先明确源码版本、九宫格兼容、JNI 接口、授权和回退路径，再进入集成；不直接替换原生库。

## 本轮执行结果与后续检查点

任务 1 已撤离并验证：30 套件、137 通过、1 正常短句缺陷显式跳过；本饿先红再绿。不是输入体验整体修复完成。

任务 2 已取得源码版 Rime 1.11.2 + Octagram 可编译证据，以及模型加载/缺失负控制与 24 组评分接口结果。发现当前定制 table 与原版加载布局不兼容：1.7.3、源码 1.11.2 均不能直接读入；详情见 `artifacts/diagnostics/2026-09-14-t9-rime-model/README.md`。

任务 3 的 291 组有无模型完整解码比较 **尚未执行成功**。下一检查点改为：取得公开完整词库来源及授权、用同源码引擎重建配套 table/prism、验证九宫格解码和读音索引，再执行控制实验。不篡改现有二进制布局、不盲目更换手机引擎、不以 24 个搭配分数设置放行阈值。

## 公开词库重建批次（用户明确要求继续）

- 来源候选选用雾凇拼音的完整默认中文表集（8105/base/ext/tencent/others），锁定上游提交、每文件 Git blob 与 SHA-256；不使用主分支浮动文件完成构建。
- 另建中文聚合入口，引用原表、保留原频率和顺序，不加入针对测试短句的词条，不启用 root 词典额外的数字/ASCII 单字符造词示例。GPL 词库仅用于隔离评估，正式分发前另核对授权和来源清单。
- 重建 table 与 prism 均用本次源码 Rime 1.11.2，九宫格保留 App 的 ADGJMPTW 编码；保留原小写全拼通路，不加内部简拼/纠错衍生。
- 固定用户反馈编码及完整码/末音节前缀，正反例先写入文件再运行。与同词库无模型、同词库有模型对照，保留无目标/目标非第一/怪词/残码失败。
- 学习只通过引擎真实选词成功提交，不直接插用户词库。若原码无法选到，完整码选择 3 次后重启，再测试用户原码；分开记录冷启动和学习后的能力，不互相替代。
- 操作检查：追加/退格、锁定拼音后的继续输入、分段、分页全局索引；这里是开源 C API 层测试，不能冒充尚未接入的 Android UI 验收。
- 本批次不替换应用 assets/.so，不安装 APK，不修改正式过滤规则。输出为可追溯重建物和测试报告。

### 本批次结果（2026-09-14，替代上文“291 组尚未执行成功”的阶段状态）

已由固定 rime-ice 五表重建 1,885,250 条、414 音节；同源码 table/prism 加载成功。291 组对照与两臂各 4680 短码执行完成，详见 `artifacts/diagnostics/2026-09-14-t9-public-dict/README.md`、来源锁及原始压缩输出。

- 反馈 12 码：冷启动两臂均 6 首选，完整码真实选择三次并重启后 7 首选；用户原始 7 码仍只有 3 首选。5 个末字前缀码在学习重启后前 5000 项仍未找到目标。
- 正向 287 行：无模型 128 首选，有模型 126 首选；4 个不期望项单独统计。模型有新增退化，不能正式启用。
- 退格两臂各 291/291；第二页首项对全局 index100 各 161 项一致、130 项 NA；不冒充 Android 后页上屏验收。锁拼音是 C API 构造输入，不是侧栏触摸。
- 严格统计读 TRAIN/BEFORE/BACKSPACE/PAGE 记录而非进程退出码；11 项诊断工具测试通过。未宣称任意词组或真机语义全覆盖。
- 后续优先末音节前缀召回、个人学习短码衔接、App 过滤链路，不继续增加词性/字数补丁。本批次没有替换应用 assets/.so、安装 APK 或提交代码。
