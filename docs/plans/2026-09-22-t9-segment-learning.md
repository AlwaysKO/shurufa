# 九宫格短码与分段学习修复计划

> **For Claude：** 必需子技能：使用 superpowers:executing-plans 来逐任务实现此计划。

**目标：** 解决用户反馈的 966 缺少“我们”、分段输入“泰鲮”记忆缺失、最近选择的“鲮”不前移；调查分段后左侧只剩标点。

**架构：** 保留 Rime 解码与原生选择索引。修复末字补全长度边界和组合会话学习生命周期；锁定/分段状态不直接插入会导致剩余码丢失的本地候选。仅成功上屏后保存明确选择，保留隐私门禁。

**技术栈：** Kotlin、Rime JNI、SQLite、JUnit/Robolectric、Gradle。

## 范围与来源
- 本次用户确认：九宫格 966、未锁 wo；泰鲮一次输入拼音后逐字选择。
- AGENTS.md：保留无关未提交修改、原签名、APK 固定交付目录、不自动安装/清数据。
- Vault《shurufa推荐交互与真机交付》及既有九宫格计划：保留字号、禁止内部过度补全、分页与原生索引一致；历史局部三键限制需按本次需求修订。
- 当前阶段：用户已授权在复核旧保护后修改、不打包；第 4 项已在实际安装 APK 的隔离原生测试中获得 `我们MA` / `真的MA` / `你好MA` 状态，现有侧栏表达式解析为空；见本地诊断 README。先补回归与收集隔离证据。

## 任务 1：966 末字简拼
文件：`android/YuyanIme/yuyansdk/src/main/java/com/yuyan/imemodule/data/completion/OfflineT9Candidates.kt`、`T9Lexicon.kt`；`com/yuyan/inputmethod/util/T9Spelling.kt`；对应测试。
1. 新增真实打包词库测试：原生只提供我哦/我时，966仍须召回我们。
2. 运行 `source ~/android-tools/env.sh && ./gradlew :yuyansdk:testOfflineDebugUnitTest --tests '*OfflinePersonalCandidatesTest*' --configure-on-demand`，确认目标断言失败。
3. 仅允许三键本地词典末字补全；保留原生首项，把补全放其后避免百条原生单字挤掉；保留跨码学习至少四键的旧边界，不放开内部音节扩写。
4. 回归三键、全码、错误内部简拼、首屏与分页、个人跨码学习。

## 任务 2：分段及锁拼音学习
文件：`com/yuyan/inputmethod/RimeEngine.kt`、`data/KeyRecordStack.kt`、`com/yuyan/imemodule/data/completion/T9CommitTracker.kt`、`PersonalCandidateRanker.kt`、`OfflineT9Candidates.kt`、`com/yuyan/imemodule/service/ImeService.kt` 及对应测试（路径均相对上述 main/java）。
1. 先测试锁拼音前后原始码、逐段实际读音、最终整词、取消/退格/宿主失败不得学习。
2. 在真实可复现失败证据后，保留完整组合与明确选择的段；成功上屏才交接整词与段证据，不将任意整词拆成假点击。
3. 对当前原生剩余候选按可靠读音的历史重排并保持选择索引；不得直接提交后半词而丢失已选前半词。
4. 验证同音单字、后页字、重复消费、数据库重开、禁个性化学习；核对两个宿主提交入口。

## 任务 3：剩余拼音侧栏
文件：`RimeEngine.kt`、`data/KeyRecordStack.kt`、`util/T9PinYinUtils.kt` 及对应测试。
1. 隔离原生状态记录按键、选前两字后的 composition、剩余键、侧栏拼音。
2. 不把静态猜测当成根因；复现后先写失败测试。
3. 修复实际解析边界，验证锁音、退格和两次分段；失配索引防护另列旧缺口，不扩入无正常路径复现的本次修复。

## 验证与交付
- 相关单测及必要全量 SDK 测试，`git diff --check`，独立审查变更和未覆盖边界。
- 手机隔离测试不使用真实用户词典/聊天内容；实际用户触摸体验单列未验收。
- 本次用户明确要求只改代码和测试，不打包、不安装，覆盖原计划的 APK 交付步骤；不更改项目长期打包规则。
- 不自动 Git 提交/推送；不把工作区其他聊天采集修改描述成本任务修复。

## 用户复核后的保护边界（2026-09-22）
- 用户要求先确认锁拼音保护与旧乱词问题的关系，再修改；本次不打包。
- 历史依据：android-integration.md 原约束明确“锁拼音/分段不覆盖原生、不把部分上屏误学成整码词”；2026-09-14个人迁移计划补了分段学习但保守清理仍覆盖锁音。Git早于精简初始化的逐次动机无法验证。
- 修复把候选资格与学习事务分开：锁音/分段仍只展示原生集合，只按同读音真实历史重排；完整码由真实按键及锁音等价键恢复，整句最终相等并匹配读音才学习。
- 只保存用户实际点选的段，不把一次选中整词自动拆成单字点击。编辑、退格、重置、失败上屏与隐私门禁仍清理/拒绝学习。

## 实施与证据
- 首轮新增966召回测试失败；更改三键查词开关后我们可召回。原生隔离引擎进一步显示位于100，追加前8项断言，不以“列表里有”代替可用位置。
- 锁音/选字仍令 unlockedT9Digits 为空，继续禁止整码本地候选介入。新增 compositionT9Digits 只供最终学习还原；区分选候选动作和未知按键，遇到显式分词/未知操作返回空。
- T9CommitTracker 保留已点选段，整文、实际读音及原码一致才交接。两个宿主提交入口使用同一整词+明确段交接；不把整词自动拆成逐字点击。保留现有存储编码长度边界（数字至少3位）、不改数据库结构。
- 分段/锁音按首个原生页现有候选的可靠读音对应编码读取个人历史，只重排，不去重/注入/过滤，也不扩大原生候选集合。当前鲮：真实未锁原生索引69、锁ling后25，均在100项页内；不承诺把100之后任意历史字提到首屏。
- 侧栏从原生组合中取第一个剩余大写键段，兼容我们MA、真的MA、你好MA、泰JGMG；不修改Rime消费码。
- 独立代码审查发现分段重排后拼音显示未跟随。隔离实际引擎先复现“鲮第一、泰jing显示”，再补按重排首项计算显示。
- 第一次相关回归：48套件219项，零失败/错误/跳过；尚非包含最后显示/前排补正的最终结果。
- 隔离验证使用已安装20260922.09 APK提供原生库/资产，以及本轮编译的类DEX补片；不构建新APK、不安装、不读真实用户词典。三轮锁tai→选泰→锁ling→选鲮均交接完整泰鲮和读音；再次输入整词首位、鲮首位且原生索引选中正确；3组侧栏例及取消通过。编译补片不是手机输入法升级。
- 诊断Context初次缺少Resources导致程序异常；修正测试容器后重跑。原生部署提示缺default.yaml，但预编译t9_pinyin能实际解码；此环境告警不计作正式应用部署通过。

## 最终验证（2026-09-22）
- `:yuyansdk:testOfflineDebugUnitTest --configure-on-demand --tests 'com.yuyan.imemodule.data.completion.*' --tests 'com.yuyan.imemodule.data.collect.*' --tests '*T9*Test' --tests '*RimeEngineCompositionStateTest' --tests '*ImeServicePersonalLearningTest' --tests '*ImeServiceKeyEventTest'`：48套件、220项，0失败/错误/跳过。两条宿主提交入口的成功、失败、不记录、密码及NO_PERSONALIZED_LEARNING均覆盖。
- 正式回归保存三键召回及100项前排、原始锁音码、分段读音校验、个人词独立召回/重开、原生候选索引/异读、不虚构整词拆字点击、剩余拼音等用例。旧防扩写/乱词及跨码边界测试未撤销。
- 最终编译类在手机隔离引擎复验：966的我们位于索引1；泰鲮三轮锁音逐字选择后整词首位，鲮从锁音原生索引25升为显示首位，原生提交仍为泰鲮；重排读音为泰ling。我们吗/真的吗/你好吗选前两字后均能继续锁ma并选吗，最终整词学习交接正确；取消不残留学习。
- 独立代码审查复核显示同步与三键插入顺序后无新增阻塞问题；`git diff --check`通过。
- 证据：`artifacts/diagnostics/2026-09-22-t9-segments/final-summary.json`、`final-regression.log`、`final-engine.log`、`display-red.log`（临时程序/JSON/日志只留本地）。
- 未运行全工程全量测试；隔离引擎不替代正式宿主触摸体验。保留原编码长度和首个原生页重排边界；未加入旧失配索引的无复现防护。没有修改真实词典或无关聊天采集改动。
- 本次按用户要求不构建APK、不安装、不Git提交或推送。旧版本当时未保存的词无法凭空恢复，后续需在包含修复的版本重新明确输入。
