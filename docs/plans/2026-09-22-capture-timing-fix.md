# 空树内容更新与输入栏冗余修复计划

> 用户要求继续修至满足要求。使用 systematic-debugging / test-driven-development / verification-before-completion；当前分支，不自动提交。沿用本次保留数据覆盖测试包授权。

目标：已确认微信聊天页面中迟到的转写/新内容不被空树丢弃；减少输入栏变化造成的重复存图。不做正文OCR、不合并不同联系人、不删历史。

1. 新增 service/capture/ScreenshotUpdatePolicy.kt 与测试：只有相同窗口/身份代次已识别为聊天时，空树CONTENT_CHANGED/SCROLLED可入合并调度。用既有ViewportDebouncer，300ms稳定等待、1200ms最大合并等待，首进仍走现有立即探测。切窗/离开清状态和待执行任务；普通内容事件不清身份。
2. 在服务接线：OCR确认页后建立短生命周期当前会话资格；内容更新走上述分支而非直接交给空树适配器拒绝；截图后、OCR后、持久化前重新检查窗口/代次，防菜单切换期间旧帧入库。补拍并发只保留一个待执行请求，不无限排队。
3. 共用媒体裁剪新增显式微信输入栏视觉适配（仅微信空树请求启用）：依据底部横向分隔边界、左右工具按钮圆环和右侧加号多点验证定位，不直接固定减高度；未匹配时保持原图，避免截掉正文。原始标题提取在此之前，文件SHA基于去输入栏后的真实图像。合成正反例先红，真机两幅仅输入栏不同图对照验证；此项不声称所有字体/主题/动画去重已解决。
4. 新增滚动事件订阅，各App仍须原聊天适配器确认，空树限当前已确认微信会话；不扩大非聊天采集。新增真实服务接线/失效回归和按钮定位小字反例。
5. 完成定向红绿、capture/service全回归、只读审查后原签名打包到固定E盘目录。安装核对实际SHA/服务绑定，用户操作转写与切聊天，助手核对新鲜日志和实际附件。迟到结果、重复静止、输入栏变更、跨会话不得仅凭单测宣布真机通过。

技术边界：像素定位是严格匹配已知微信工具栏而非通用语义识别；不确定时保留。GIF内部动画、全屏图片和特殊布局的语义去重需继续独立验证。不为降低存图数量使用整图相似度阈值。

## 第一轮接线验证（2026-09-22）

- 22 项定向回归先出现 3 个预期失败（空树内容被拒绝、滚动未订阅、媒体仍保留输入栏）；接线后 22 项及 1 项本机真实样本测试通过。已有弃用 API / Gradle 警告未消除，不属于本任务。
- 两张仅输入栏不同的旧正式缓存，真实 Kotlin `wechatInputBarTop` 均返回 2496；裁后像素完全相同、WebP 编码字节相同。真实截图与本机专属测试只留 ignored diagnostics。此证据只证明已知单行加号布局，不代表所有输入栏布局去重。
- 只读审查另发现：首次 pending 身份确认期间唯一一次内容变化未记账，确认帧只识别标题而首帧仍被保存，可能漏末次状态。正在按红绿补同 scope 在途 dirty 标记：未确认仅记账，确认成功才补拍，不扩大未确认页采集权限。

## 审查补充与边界收尾

- dirty 缺口先用 12 项定向测试复现其中 2 项失败，再修正；包含工具栏反例、导航清理在内的 259 项 capture/service 回归通过（0 failure/error/skipped）。只读二次审查未发现新增重要遗漏。
- 核对通知入口发现旧 `notificationFallbackBounds` 仍固定取高度 3%..90%，因此新工具栏视觉适配即使传入也看不到真实底栏，且可能丢最后正文。收尾统一空树截图边界：只排除系统已知状态栏；底部先保留全窗口或真实键盘边界，再由已验证工具栏定位裁剪。未知布局不裁，不把边界调整误称语义去重全部解决。
- 排除系统状态栏也避免时钟变化成为聊天内容指纹；标题仍完整保留，OCR 标题带使用相对截图原点坐标，不重复去掉标题。增加已含系统 inset、无效 inset、键盘边界和通知保留末行回归。

## 最终离线验证

- 系统栏/通知边界 20 项定向中 2 个预期红后修复；最终 capture/service **260 项全通过，0 失败、错误或跳过**。原有负例图像解码输出 unimplemented 不对应测试失败；弃用/Gradle 警告未在本任务顺手修改。
- ignored 本机真实样本 **2 项通过**：除工具栏 top=2496 的定位与像素/编码一致，还实际通过 `WindowMediaCapturer` 处理两张图，最终高度 2370（2496−126）、SHA 相同；移交 OCR 的标题原始像素与原图 y126..269 的 143px 区域完全一致。未使用正文 OCR。
- 通知入口键盘可见会由既有 `shouldCaptureNotificationFallback` 拒绝，因此其边界 helper 传 null；这不是对所有 OEM 窗口/键盘场景的真机保证。三次只读审查的补充点已处理，边界收尾未发现新增阻断问题。
- 新 APK 构建/安装与用户转写、静止、输入栏、跨会话真机验收仍按实际后续证据记录，不由以上测试替代。

## 修复包交付及安装（2026-09-22）

- 推荐本轮修复包：`E:\Projects\shurufa-android\apk\shurufa-2026-09-22-v20260922.12-2026092212-debug-45790417.apk`。版本仍为按小时生成的 20260922.12 / 2026092212，不能仅凭版本号与之前 6c7807cf 诊断包混淆。
- 源 APK、E 盘副本、手机已安装 base.apk SHA256 均为 `4579041796f105a605bd64994e4582fcd4f34d20f62f18018b8439806c2d16c0`。原证书 API23/27/28/32/36 验证通过，非 testOnly，minSdk23 不变。
- 已沿用本轮授权执行 `adb install -r` 成功，未卸载或清除数据；默认输入法不变，采集授权 `reporting_consent_v1=true`。无障碍服务实际已绑定，并订阅 TYPE_VIEW_SCROLLED。安装记录在 ignored `fix-install/`；初次授权键检查使用了错误的独立配置文件，随后依源码读取默认 preferences 验证 true，不将前一次缺文件当作未授权。
- 安装后手机 `mWakefulness=Dozing`，未操作解锁/微信页面/转写；本轮新包转写、静止、输入栏切换、跨会话及标题准确率 **尚未真机验收**。安装后读取的专用 trace 仅有缓冲区标题，不能拿旧诊断包日志声称新策略运行成功。
- 已请用户解锁并打开指定测试聊天，暂不转换，待核对初始帧后再操作。下一步应先取转换前正式附件和新鲜日志，再请转换，对照末次附件、重复入库和会话归属；标题模型仍为 ML Kit，未将 tiny 的两样本结果冒充总体准确率，也未声称内存目标已达成。
- 工作区保留修复与文档，未自动提交/推送；任务未标记整体完成。

## 用户打开后的修复版真机初查

- 用户“已打开”后，Windows ADB再次核对安装 SHA 为 45790417 完整修复包，版本与默认输入法不变。取证目录 `fixed-before-133008`（WSL时间，与手机日志时钟有偏差）。
- 当前“吴驰峰”聊天的3秒语音 **已经展开转写文字**，所以不能把当前帧称为“转换前”。正式偏好指向 ca6db09156cd36eb39b62c735dfa9bf0820d70c5181530ccaaba2e0abc721be9，缓存文件SHA核对一致；实际附件可见完整转写文字，已排除状态栏及已识别输入栏。此项是新包已有转写状态保存证据，不证明转写动作发生时的前后两个状态都被捕获。
- 新鲜手机日志13:34:01..07中，5个CONTENT_SCHEDULED合并为2次截图检查，均进入DUPLICATE，没有该片段的新INSERTED；证明本样本空树后续事件已走新分支，不能推广为所有静止/输入栏场景通过。
- 下一步需用户先收起现有转写（若微信支持），或选择尚未转换的语音，先核对未展开基线再转换；不自动点击或播放语音。

## 未展开基线已就绪

- 用户“准备好了”后再次核验手机安装完整 SHA 45790417，当前3秒语音下方已无转写文字。取证 `ready-before-133212`。
- 已取得并校验正式缓存8972e4a10fc8953a0202aac62e274f20905dec22c93999c90ea67e1043db7527，图中无转写；仍有右侧滚动条，不能将滚动条引起的额外变化称为已解决。日志13:35:27.663和28.582各有INSERTED，中间一次DUPLICATE，并观察到CONTENT_PENDING。
- 当前偏好的最新SHA2250b28c63447b4a7b68e1d9a15be891fc0365a6465187e7ea13538abf4fee32不在手机缓存；本地server/uploads/chat/22也无对应文件。未据此断言丢图或上传成功。8972缓存未与某条INSERTED逐条绑定；保留日志、SHA及屏幕基线供转换后对照。
- 接下来请用户仅转换该3秒语音，保持此页面不滚动，再读取转换后的实际附件与新鲜记录。

## 本轮转换后的真机核验（未整体验收）

- 用户“已转换”后再次核对实际安装 SHA45790417。取证目录 `converted-after-133445`；实时日志在 `ready-before-133212/live-conversion.log`（600秒有界读取，不代表无限后台监控）。
- 未展开正式缓存8972...与展开正式缓存94a38145cfb2ff3f8d4cdad49016177b5990873c5bb4422ae2d4e4e25e494c65均已实际看图，后者含完整“测试123123。”，并与当前已保存偏好SHA严格一致。此轮前后状态采集可确认；后台对应附件展示、总体迟到转写率仍未逐条验收。
- 新日志手机13:38:00.683、01.097、01.647、24.689、46.001有SERVICE INSERTED；48.526为DUPLICATE。不能把五次INSERTED均宣称必要或均宣称重复，部分中间附件已不在缓存，需单独核对。
- 已证实另一个冗余来源：旧展开基线 `fixed-before-133008/screen.png` 与本轮展开屏幕原始PNG比较，正文y269..2496、输入栏y2496..2664像素 **完全相同**；标题栏仅x96..156、y168..228（2912像素）不同，视觉为左上角未读数字消失。相应正式缓存ca6db091...与94a38145...具有不同SHA。两份有损WebP解码后正文出现低幅度编码差异，不能用解码WebP的正文精确hash冒充原始像素精确去重。
- 结论：本次展开文字已捕获；同内容去重 **未完全达标**，仍须处理标题栏未读数字及滚动条等非正文变化，标题识别准确率/内存亦未整体验收。不将此测试记为用户全部要求完成。
- 下一修复需使用压缩前的精确内容判据并保留标题/窗口/会话隔离，不靠粗糙整图相似度，也不把不同联系人合并；具体方案先验证同正文换姓名、单像素新正文、导航取消和未确认页反例。不能直接裁掉整条标题栏或按WebP解码近似阈值跳过图片。

## 后续未读数字去重修复

用户继续后已实施“压缩前正文精确像素＋标题文字精确像素＋当前已确认scope”的补充去重，保持保存图片不变。268项回归及私有真实三状态回放通过，5364098f版本20260922.13已保留数据覆盖安装。实现、跨帧隔离、完整SHA及待真机复验范围见 `2026-09-22-unread-badge-dedup.md`；不将前一版45790417的转写实测冒充新包全部验收。
