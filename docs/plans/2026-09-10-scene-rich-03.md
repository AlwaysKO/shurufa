# 极简线条与真实动物 scene-rich-03 实现计划

> **For Claude：** 必需子技能：使用 superpowers:executing-plans 来逐任务实现此计划。

**目标：** 继续生产「你先跑、不高兴了、你认真的、被你发现了」四款原创真实 GIF，仅评审归档，不加入 APK。

**架构：** 复用 scene-rich-01/02 的固定四词批次验证和四姿势渲染链，只新增 scene-rich-03 白名单。比起泛化任意批次或另建渲染器，此方案保留隔离边界且改动最小。两张极简线条通过角色关系与动作抖机灵，两张真实动物通过怀疑/心虚眼神制造反差；参考情绪机制而非照搬角色构图。

**技术栈：** TypeScript、Vitest、sharp、确定性中文字体、原创 2×2 四姿势 PNG。

## 任务 1：测试与最小扩展
- 修改 `server/src/expression/sceneRichRenderer.test.ts`：03 四词正例；混入旧词、旧 ID 负例；03 CLI 正例、04 未登记负例；03 合成测试母版真实渲染。
- 运行 `cd server && npx vitest run src/expression/sceneRichRenderer.test.ts`，先确认03因未登记失败。
- 修改 `server/src/expression/sceneRichRenderer.ts`：新增固定03四词和明确CLI错误文本，不改变01/02行为。
- 重新运行上述定向测试，并运行 `npm test`、`npm run build`。

## 任务 2：母版与成品（主代理）
- 创建 `assets/expression/batches/scene-rich-03/manifest.json` 与 `masters/scene-rich-03-*.png`，每项四个原创动作关键姿势，不用单图缩放冒充动画。
- 极简线条：你先跑（双角色追逐）、不高兴了（被戳脸不满）；真实动物：你认真的（狗的怀疑眼神）、被你发现了（猫的心虚眼神）。没有知名角色或真人贴脸。
- 运行 `cd server && npx tsx scripts/render-scene-rich-01.ts --batch scene-rich-03`。
- 检查 `artifacts/expression-batches/scene-rich-03/report.json`：4/4、240×240、16帧、1600ms、循环、每张低于250KB、首帧WebP一致。
- 视觉查看完整四姿势与成品联系表；归档至 `server/images/` 并保留 review-only，统一 APK 接入留待后续。

## 任务 3：审查与交付
- 实现后依次规格审查、独立代码质量审查，修复阻断项后复验。
- 检查本次文件归属及 git diff，不纳入既有 client 修改，不创建 worktree；由主代理统一提交。
