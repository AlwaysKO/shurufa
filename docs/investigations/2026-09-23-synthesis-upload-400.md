# AI 合成底图上传 400 排查

用户报告 `POST /api/v1/dashboard/synthesis-library` 返回 400，并要求以后在本地自动执行 SQL。

- 修改前本地 `synthesis_asset` 已存在，13 个字段齐全，暂无缺表/缺字段证据。
- 确认数据库主机为本地后，调用现有 `migrate(pool)` 执行 001～029 全部迁移，成功；未调用素材清单导入或修改线上数据库。
- 本地运行中的 3000 接口：无确认字段的既有 GIF 返回 200（重复）；临时变体新图返回 201（实际入库），随后只删除该临时测试图，清理返回 200。
- 400 在文件或区域校验阶段可返回，但原前端 `post()` 抛弃了服务端 `error`。已对底图上传保留该错误说明，无有效 JSON 时回退到状态码。
- 用户失败的原 GIF 尚未提供，不能认定具体尺寸、帧数或文件损坏；已询问原文件路径。前端刷新后再次上传将显示具体原因。
- 本地自动执行并验证 SQL 的长期要求已写入 `AGENTS.md`。
