# 2026-09-22 九宫格分段问题隔离诊断

- Windows ADB 确认安装包 `com.yuyan.pinyin.offline.debug`，版本 `20260922.09`；当前默认 IME 为该包 DebugGifImeService。
- 使用实际安装 APK 和其 libyuyanime.so、内置 Rime 资产，在独立 `/data/local/tmp/shurufa-t9-0922-*` 目录测试；不安装/切输入法，不读取或清除真实学习数据库，不操作聊天界面。
- APK SHA256 和原生状态摘要见本地 `summary.json`；完整日志见 `native.log`。这是引擎及解析表达式证据，不是用户触摸全链路验收。

## 结果
1. `966` 原生首选我哦，comment=wo'o；本地候选层三键禁末字补全，可导致我们无法独立补回。已添加正式回归测试。
2. `8245464` 选择泰后原生余段 `泰JGMG`，鲮位于当前原生页索引69，最终可提交泰鲮。本次隔离原生路径本身没有缺字。
3. `9663662` 选我们后 `我们MA`；`94363362` 选真的后 `真的MA`；`6442662` 选你好后 `你好MA`。原生仍有最后一字，现有侧栏表达式按单引号切分再要求整段全大写，均返回空；UI空拼音时显示标点。可稳定触发与第4项一致的缺口。
4. 学习链路静态缺口：selectPinyin清tracker；unlockedT9Digits在锁音/候选选择后返回空，关闭应用层个人候选重排。不能仅凭本次隔离空库推断真实设备泰鲮记录具体是否存在；需覆盖锁音、分段、成功上屏后再读个人词。

上文为修复前诊断。用户随后授权复核旧保护后修改，并明确本次不打包；最新结果见下节。

## 最终修改与复验
- 三键只开放合法词库末字召回，保留原生首项，补全紧随其后；不扩大三键跨码学习、不修改词库或乱词黑名单。
- 锁音/分段不开放整码本地候选；仅最终学习还原等价原码，整词与逐段真实读音匹配且成功上屏才保存。原生首个100项页内重排保持原生索引；实际鲮在锁音后原生索引25。
- 220项相关测试通过。隔离原生引擎：我们从仅补回时的索引100调整到1；泰鲮三轮锁音逐字选择成功学习，鲮前移并保持整词提交；剩余侧栏和显示同步通过。
- `display-red.log`先复现鲮第一却显示泰jing；`final-engine.log`修后泰ling并ALL_PASS。
- 测试用实际安装20260922.09的库/资产，加本轮编译类DEX，不构建新APK、不安装、不改真实用户数据。测试Context的Resources异常已修正重跑；预编译Rime可用但隔离部署缺default.yaml告警保留，不冒称应用部署验收。
- 汇总见本地final-summary.json；完整范围、命令及边界见docs/plans/2026-09-22-t9-segment-learning.md。
