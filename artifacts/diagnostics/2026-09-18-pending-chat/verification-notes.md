# 验证边界

- Android 本轮从用户已验证微信/抖音有截图的21诊断包源码继续修复，不回滚此前 `.get()` 补探测代次及完整窗口顶部裁剪。
- QQ适配器/识别规则不改。默认共享标题跟踪器不启用微信的短时空帧连续性选项；确认重试签名及输入框点击边界也显式排除QQ。
- 移除临时诊断代码/UI/Toast退出等待耦合；被移除的专用诊断代码和诊断测试留存在 removed-diagnostics/，正常回归测试保留。
- 后台只增加虚拟待确认聚合，无数据库迁移、真实数据合并、删除或重写。图片仍通过真实conversation_id操作，不整组冒充联系人。
- 数据库验证只运行server/scripts/test-chat-continuity.sh及test-chat-images.sh创建的专用initdb实例。实例校验data_directory和数据库名后才能写入，结束停止自己的实例并保留；未读取业务连接配置。
- 前端全套测试需要排除 `.runtime` 内历史交付副本。第一次宽路径命令匹配到旧副本测试，因副本缺AndroidManifest而失败；实际client/tests在排除该目录后通过。不为消除旧副本错误删除/修改无关文件。
- 浏览器拦截全部/api/与/uploads/请求，用61张夹具验证待确认单入口、分页、图片切换、选择、刷新状态和按来源合并选择；禁止向真实接口写入。桌面和390px无横向溢出。当前工具不支持查看图片输入，未冒称已人工看过生成截图。
- 前端构建保留既有大于500kB的bundle体积警告；不为本任务做无关拆包。
- 手机不能连接，不安装、不清数据。打包校验不替代真机截图/归类验收。
- 线上前后端未部署；虚拟入口需前后端一起更新，不是单靠安装APK改变远端页面。

## 独立审查
- 审查P2：连续状态保留后，空标题首图P恢复A时缺previousKey。已补独立红测并修正，独立审查复核未发现补丁新增P1/P2；同时覆盖中间P不能改写原A首图指纹与同名不同字形隔离。
- 审查指出的无树入口/OCR仅标题门控属于21包原有逻辑，与本轮before备份一致，不是本轮放宽。该门控仍有可能把非聊天标题误当聊天的既有风险，需要真实页面证据另行校准；本轮不能无证据添加强制图标OCR/输入框条件破坏用户刚验证恢复的截图。未扩大其放行条件，授权/窗口/锁屏保护保留。
