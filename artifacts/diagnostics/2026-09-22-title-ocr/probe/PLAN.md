# 独立离线 OCR 诊断
用户已在 2026-09-22 明确允许安装，不覆盖输入法。无 INTERNET 权限，输入仅两张已保存的顶部图，输出私有文件。
1. 纯 JVM 红/绿测试固定裁切、标题选择、不按正确答案纠错。
2. 官方 SDK 固定昨日 revision，仅实验应用引用；三个引擎分进程启动。
3. 同图比较原始 216 与完整 270 高度；可选顶部 WebP82 压缩对照不冒称全屏压缩基线。每项 3 次，记录所有原始行、框、选中标题、耗时、PSS 采样。
4. 构建签名核验与无联网权限检查后安装独立包，不改正式应用及聊天数据。
5. 采样 PSS 非绝对峰值；两张图非整体准确率，不把已有缓存命中视为原始 OCR 正确。
