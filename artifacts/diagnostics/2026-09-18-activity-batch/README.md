# 行为明细模式联动与本页批量删除（2026-09-18）

## 用户已确认范围
- 显示底层事件仅临时切换原始视图，取消恢复此前整段/原始偏好；加载时不显示旧模式数据。
- 每行勾选、本页全选、全不选、删除选中。全选仅本页20行；翻页、模式、用户或筛选上下文变化清空选择。
- 整段模式提交整组全部原始ID，原始模式提交每行单ID；系统确认窗口明确组/行数与原始条数，Enter确认、Esc取消。
- 单次事务核验全部范围，任一目标不存在、范围变化/混入其他用户或DB异常则整批拒绝/回滚。上限20行、总计10000原始ID，不按筛选条件执行清库。

## 实现
- Activity.vue保留用户模式偏好，底层开关仅影响有效模式；增加本页选择，确认/删除去重，操作前后核对用户/筛选/请求版本；失败保留列表和选择，成功重新加载，末页自动回退。
- api/index.ts新增deleteActivities，一次带明确快照及当前用户的POST请求。
- activityDeletion.ts新增/events/delete-batch，仍保留原单行接口。使用相同GROUP_KEY、按ID排序锁定全体实际成员，逐组比较快照后一次DELETE并核对行数。
- 仅6个相关源/测试文件修改；起点副本和this-change.diff用于区分并行/先前未提交改动。

## 验证
- TDD前端新增/更新行为最初11失败，修复后前端全量133项通过；新增8项批量数据库用例，18项单条/批量删除及6项APP名称回归共24项独立PG测试通过。总计157项通过。
- 修复既有单条删除测试注入trigger未清理的问题：原先它是文件最后一例，追加测试后跳过删除的trigger污染后续用例；现finally仅清理独立实例内该测试自己的trigger/function。未涉及业务数据库。
- 前后端构建通过；前端现有大包体积警告未扩展处理。
- 浏览器全API内存夹具检查两种模式恢复、加载隐藏旧行、20行本页全选/清空/取消、Enter/Esc、409保留选择、删除后仅保留页外行、末页回退、原始模式单条范围与390px操作入口。无原生确认、无页面异常、无真实删除。
- 专用脚本：bash server/scripts/test-app-names.sh server/src/api/deleteEvents.postgres.test.ts，每次新建initdb、独立Unix socket与数据库，核验data_directory后才写入；结束停止自己的实例并保留产物。
- 未提交Git、未部署线上，不修改Android和其他模块、不清理真实记录。所有权核验见ownership-check.txt。
