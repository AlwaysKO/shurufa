# 独立只读审查

按 requesting-code-review 流程审查可视诊断及截图交接，未委派源码编辑。

已处理并由原审查者复核：
- 历史120条在进程首次记录时不再减半。
- 待提示到期时间固定，连续截图不无限推迟；错误不被普通镜像等待覆盖。
- API30+等待Toast隐藏回调而非猜测150ms，1秒超时幂等失败。
- 系统未确认显示且无回调的Toast超时后取消并退役，暂停弹窗但保留诊断，后续截图不永久锁住。
- 已确认显示的Toast不能把超时视为隐藏；迟到shown重新绑定保护。

审查只确认源码修正；单测/构建结果单独见运行日志，不能代替目标手机的提示、裁剪及上报验收。
