# 词云空白修复（2026-09-18）

- 根因：load在loading仍为true时调用render，v-if词云容器未挂载，初始化直接跳过。切换范围也会移除旧容器，但旧ECharts实例未释放。
- 改动仅WordCloud.vue与新增wordcloud.test.ts：成功后先退出加载状态、await nextTick后绘图；用template ref定位当前容器；请求版本防迟到成功/失败覆盖；加载/卸载释放实例、resize重排；失败可重试。
- 保留既有接口、1~10字符过滤、Top词频与点击明细规则，不改服务端和真实业务数据。
- 新增9项回归；初始7失败2通过，修复后9项通过；临时移除nextTick再次7项失败，恢复后全前端123项通过。前端构建成功，仅有既有大包体积警告。
- 真实浏览器与实际echarts-wordcloud插件验收12组（1440/820/390px × 7/30/90天/全部），验证画布存在实际词语；另验空态、失败重试、文字点击跳转与返回重建。全部API拦截为夹具，无业务数据库连接。
- 未提交Git、未部署线上、未清理用户记录；保留其他未提交改动。截图仅存档，验收依据为真实渲染对象和交互断言。
