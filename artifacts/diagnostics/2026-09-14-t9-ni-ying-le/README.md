# 你赢了：同码歧义与配套词库过滤

2026-09-14 用户反馈nixing（待选拼音）。未获取用户本次手机原始按键日志，按已明确习惯测6494645与64946453。

公开固定词库原文ext.dict.yaml包含“你赢了 ni ying le 100”。原版开源引擎配套同源词库的首100原始结果：末字首字母第4、全拼第2；“你醒了”也是合法同码候选（ying和xing都对应9464），不能强制用某个词替代另一个词。

现有App配套130860条词表不含“你赢了”；trusted的整词收录检查会拒绝它，与原生是否有读音路径是两个问题。本轮引入整词精确索引，范围限定公开原表中2–30个中文字符的原词条，未根据本轮案例补词，未生成句子。源锁沿用前批五表，所有文件校验后生成。

发布与验证状态待本轮最终结果补充；不把C API结果当成手机上当前闭源库的原始候选快照。

## 实验结果（并非已接入手机版）

- 公开整词索引1855331条、29820648字节，排序UTF-8原文+偏移，精确二分，不做句子生成或频率估计。
- 构建Python3项先红后绿。Android精确索引3项通过；你赢了召回/后页/三次学习双码优先测试通过；原先跳过的3句正常短句保护测试通过。
- 广泛测试有1项失败：公开原文含“夜魔”，会低位出现，但旧测试要求不出现。“怎么”首位仍通过。未修改这一失败期望来伪报通过。
- 已向用户询问低位真实低频词的处理偏好；未答复前不保留索引生产接入。`prototype/`为实验实现与单测，`public-index-integration.pending.patch`为待接入改动，不是应用中的生效代码。该patch还需复制原型类、生成资产、处理公开词条与旧测试的策略冲突；不是可直接发布的一键补丁。
- 29.82MB索引只留/tmp/shurufa-ni-ying-le/public_phrases.t9idx，不加入正式assets；其哈希与源锁在public-phrase-sources/。随附原许可证和源表头，未改变项目其他代码许可。
- 未安装APK、未验证真实APK内存映射/手机性能。布局偏移校验不等于正文任意损坏都可检出；数据源在构建时校验。

当前源码实际保留：取消待选提示，只显示已对齐部分，残码留在内部；回车保护不变。关系推荐当前关闭，但其异步入口也改用Kernel.isComposing，不再从显示为空推断可以覆盖候选。

当前保留版本重新验证：隔离Gradle12套件86项，85通过、1既有短句缺陷跳过、0失败；见retained-test-results.json和retained-green.log.gz。此结果不包含待接入原型，不代表候选问题已解决。

## 后续状态更新

用户已明确允许真实低频词低位出现，并要求继续执行。公开索引现已正式接入源码和debug APK，不再只是本目录原型。见 `../2026-09-14-t9-public-integration/README.md` 的最终测试与打包证据；本页上方暂撤离记录保留为历史，不代表当前状态。
