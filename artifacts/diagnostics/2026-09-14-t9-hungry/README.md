# “我饿了”首次候选误删诊断

日期：2026-09-14。用户提供截图显示 `wo'353`、首选“我”。

- 手机已安装版本 `20260914.10`，更新时间 11:01:30；APK SHA-256：`d76570832c4ae9f2ecd2842e247bca7c6367a9f85959b2afd6b3c57567ec07cc`。
- 复用 `../2026-09-14-t9-rehearsal/T9Corpus.java`，使用该 APK 的引擎/词库/后处理，在手机独立 app_process 和新测试目录执行。未读取真实个人词库，未操作聊天输入框，未重新安装 APK。
- `96353` 原生首选“我饿了”，随后“我饿”“我”“握”“窝”；后处理删除前两项，首选变为“我”。
- `T9Spelling.preedit` 只按当前首选读音 `wo` 消费前两码 `96`，剩余 `353` 被保留，形成 `wo'353`。数字不是插入的候选文字，也不是数字键盘模式。
- 对照“真的吗”“玩漂流”同样为原生首选被删除；“用得上”仍被本地补回、“总额而”仍被剔除。证明不能简单取消全部过滤。

结论：这是尚未替换的整词白名单误删正常短句，不是记忆次数保存故障，也不是底层不会解码。此前 133 项单测包含个人学习后的回放，不证明空历史首次语义召回通过。

本次仅诊断，未修改生产候选规则、词库或 UI。下一阶段验收必须同时覆盖空历史正常短句、错误拼接及个人词；不得用隐藏剩余数字或单补“我饿了”替代修复。
