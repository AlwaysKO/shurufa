# 整句候选恢复：2026-09-14 用户接受取舍后的版本

## 本次决定及范围

用户用搜狗截图明确要求：输入整段后，候选中直接出现完整“我不再彷徨”，一次选中，不仅是显示完整拼音。随后明确接受“完整句子偶尔用词不准，但不再因整句未收录只剩前半句”的取舍。

本轮只改 `OfflineT9Candidates.kt` 一个生产文件，精确增量见 `scope.patch`。现有 .17 显示修复、JNI、词库、提交与学习代码保留，不恢复词性/字数规则，不为反馈句子补公共词条。

### 实际策略（不是新语言模型）

- 没有固定词库可信完整候选时，保留原生完整解码，按照原生顺序放在分段前缀之前。
- 必须有读音、纯汉字、汉字码点数与音节数一致，并且匹配全部按键；只允许末字前缀，不能内部简拼、附加符号、错码或残码冒充整句。
- 固定词库已有完整词/末字补全时仍保留原来过滤，防止已知“充电宝”“用得上”等被未知组合挤掉。**因此这不是全面取消整句门禁**：同码固定词可能仍挡住另一条未知整句。
- 个人词只影响优先级，不关闭其他原生整句；否则先选错一句后就不能纠正。
- 拼音跟随恢复后的首选，候选保留原生索引；实际点击仍走原来的 Rime 选择路径。
- 只保留原生解码结果和排序，没有接入新的上下文模型，不声称达到搜狗的识别准确度。

## 验证

22套件、125项测试，0失败/错误/跳过，构建成功。测试原文在 `final-xml/`，构建过程在 `green-build.log.gz`。

- “我不再彷徨”：完整码 `9628924726448264` 和末字 h `962892472644` 均恢复首位、原生索引0、完整对应读音。后页索引、分段前缀、缺读音/错码/混杂字符检查保留。
- 先学“我不在彷徨”3次并重开，仍能在首屏与后页选另一整句；再选择“我不再彷徨”5次并重开，在两种码下置顶。这里验证本地学习接口，不是实际手机触摸学习。
- 305组录制语料与上轮 `2026-09-14-t9-long-composition/replay.json` 比较：此前150行正向正确首选全部保留，新增23行正确首选，共173行。130行首选或目标名次变化，**不能将130都称为语义改善**。详细变化见 `replay-changes.json`。
- 充电宝、怎么、你发货吧、用得上、真的吗完整码、玩漂流完整码、我饿了的对应旧正确首选保留；明天去爬山完整码新增为首选。你赢了仍有同码候选排序歧义，不声称未学习前必定第一。
- 录制的长句逐键正/逆序快照继续检查候选非空、前缀可选、索引正确，完整码与末字前缀恢复整句。没有伪称快照回放就是现场按键/退格。

## 明确保留的失败/风险

- 原生解码语义仍可能不自然，例如“想吃过户”“我去闭合”；一些末字前缀样例仍找不到目标。本次解决的是整句被删除的问题，而不是整体语言模型准确度。
- 在故意移除固定词库的测试中，原生“总额而”会进入整句回退；正式词库中的“用得上”使这条码仍走既有可信完整词通路、拒绝该乱串。不能保证数据缺失时既保留所有整句又永远没有怪词。
- 原来5个绝对拒绝未知组合/中间态的测试与新取舍冲突。没有删除案例：保留原始失败 XML 于 `initial-xml/`，测试改为明确的新结果、字音/索引/分页一致性与不自然候选记录。语义反例位于 `OfflinePersonalCandidatesTest` 的 `NATIVE_SENTENCE_COUNTEREXAMPLE` 输出和长句逐键输出中。

### 红绿与审查

1. 新整句测试4项，旧实现2项失败（候选只有前缀），见 `red.xml.gz`。
2. 第一次实现后125项中5项旧策略断言失败，见 `green-initial.log.gz`；305语料的旧正确首选没有退化。
3. 只读复审发现“个人整句关闭其他整句”问题，新增测试先确认1项失败（`learning-red.xml.gz`），然后修正仅由固定词库决定关闭回退。最终125项通过。
4. 复审未发现新的阻断项，但明确指出固定词同码冲突和语义准确度限制。未因用户接受不确定性而取消拼写检查。

## 真机与交付边界

本轮 ADB 多次返回空设备列表，已请用户重新连接；没有现场 JNI 运行、实际一次点击提交或微信 UI 验收。旧手机 JNI 快照仅作为自动测试输入，不冒充新 APK 实测。**未安装、未提交、未操作聊天或读取用户输入历史。**

APK：`android/YuyanIme/app/build/t9-display-audit/outputs/apk/offline/debug/yuyanIme_2026091423_debug.apk`

- versionCode：2026091423
- 大小：129724764 字节
- SHA256：`bb7deae0178d864efe0ac95ed31527c21bd02b6f1d77e2c6b346ca526b94bf34`
- 签名验证见 `apk-signature.log`。
- `.so`、table、prism、公开索引及压缩方式与 .17 相同，见 `package-verification.json`。

手机重新连接后的下一项验收：新 APK 隔离目录分别使用空历史输入上述两码，检查首选、读音、原生索引0一次选择的实际 commit，然后验证真实候选点击；不得把本轮测试通过当成这项已经完成。
