# 时间线日期裁切与北京时间修复

- 根因：横轴直接截取经 pg 序列化后的日期时间字符串，标签冗长，45度旋转搭配30px底部空间导致裁切；三个统计接口按数据库会话时区分组。
- 每日接口显式按 Asia/Shanghai 分组并输出 YYYY-MM-DD 文本，避免 date 被转为带偏移的时间戳；小时/星期提取也使用同一时区。保持原有近N天滚动筛选范围、字符类型与用户隔离，不回写历史数据。
- 横轴水平显示 MM-DD，自动抽稀与防重叠，网格包含标签；tooltip保留完整日期。页面标注北京时间，窗口resize更新三图，卸载清理。
- TDD：前端3项红转绿，真实独立PG9项在修复前7失败2通过、修复后全通过。UTC/上海/洛杉矶数据库会话测试含北京午夜和周日跨周一，以及其他用户与时间范围隔离。
- 前端全量114项通过；前后端构建成功（前端既有大包体积警告仍在）。真实浏览器全API夹具验收27组：UTC/上海/洛杉矶、1440/820/390px、7/30/90天；检查ECharts真实文字变换后边界，水平、不重叠、不裁切。
- 仅改Timeline.vue、dashboard.ts三个接口，新增两个回归测试及专用PG脚本；保留现有未提交改动。未读写真实业务记录、未提交Git或部署线上。
