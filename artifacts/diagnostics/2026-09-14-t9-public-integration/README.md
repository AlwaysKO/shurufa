# 公开整词索引生产接入与验收记录

2026-09-14。用户已明确允许“夜魔”这类真实低频词低位出现，并要求继续解决输入问题。本批次已接入源码并生成debug APK；未安装手机、未提交代码、未公开发布。

## 实际改动

- 使用前批锁定rime-ice五表生成的1855331条精确整词索引（29,820,648字节），不补测试词、不生成句子、不更改原生解码或词频。
- `PublicPhraseIndex`直接映射未压缩APK资产，UTF-8精确二分查询，不在Java堆创建百万词集合。源锁、索引哈希、原许可证及源表头随包保留在completion/public-phrase-sources。
- App首屏/后页过滤增加公开原词收录依据，仍须匹配真实读音与输入码；新增公开词置于已有可信整词/末字补全之后、分段前缀之前。个人学习仍优先。
- “夜魔”低位允许，但“怎么”首选测试保留；“充电吧”低位允许，但“充电宝”首选测试保留。
- 没有替换原生.so/table；APK内arm64 libyuyanime.so哈希与前批基线一致。之前的末端补全原生实验没有混入此APK。

## 验证结果

- Python构建工具：3项通过；前批已有先红后绿记录。
- 隔离Gradle：15套件92项全部通过，0失败/0跳过。原先忽略的“我饿了、真的吗、玩漂流”原生候选误删测试现已解除并通过。
- “你赢了”：首字母末码6494645和完整码64946453均可保留，后页索引不丢失；完整码选择3次、关闭数据库再打开后，两码均第一。这是存储重开，不宣称额外完成了Android进程重启/真实触摸验收。
- 缺失索引回退既有词表测试通过；格式/偏移损坏单测拒绝加载。没有声称正文任意损坏都能检出。

### 305组真实App过滤层重放

数据取自前批固定开源C API候选录制：291语料（287正向、4不期望项）、12反馈编码、你赢了2码。案例和首100原生候选逐行保存到 `src/test/resources/t9-public-native-replay.tsv.gz`；SHA-256 `415021da9083d05ca05c30aabca0041d11b1ddfdb0ebf52cac80720307daff7f`。不是当前手机闭源JNI或UI现场录制。

同一份候选、空学习库，在App内分别禁用/启用公开索引对照：

| 分组 | 原过滤第一 | 接入后第一 | 原过滤召回 | 接入后召回 |
|---|---:|---:|---:|---:|
| 287正向语料 | 57 | 138 | 58 | 145 |
| 12反馈码 | 9 | 12 | 9 | 12 |
| 你赢了2码（冷启动） | 0 | 0 | 0 | 2 |

没有丢掉原有正确首选；测试列出的总额而/总额儿/冲屌啊/灭除妈啊/本饿/饿喇叭/饿牙齿/饿雅典/饿我不没有进入首屏。**这是有限已知反例检查，不代表所有乱词都不存在。** 测试中的这些集合不是生产黑名单。

“你赢了”冷启动仍与同码“你醒了”等竞争，不能保证未学习时永远第一。287正向中仍有未召回或未排第一的项；此补丁解决已有原词被误删，不负责生成任意正常句子或保证所有末字补全。

## APK

路径与完整哈希见 `package-verification.json`：

`android/YuyanIme/app/build/t9-display-audit/outputs/apk/offline/debug/yuyanIme_2026091415_debug.apk`

- 129,719,044字节；SHA-256 `580ef8fd36972731d3ce269e92c3130a7ecbec7b3475619d5f39d917e8349fa6`。
- 实际ZIP内索引方法0（未压缩），长度、条数、SHA-256与源资产一致；许可证存在。
- apksigner校验成功，v1/v2有效；工具同时输出META-INF条目相关警告，原文保留在apk-signature.log，没有隐藏或为了消除警告改动无关内容。
- 真机映射、启动开销、真实连续按键、软/硬键盘回车和当前手机输入体验尚未验收。构建成功不替代真机验收。

## 证据与复跑

- `summary.json`：套件和分组计数；`replay.tsv/json`：全部305条前后名次；`integrated-*.log.gz`：完整Gradle日志。
- 使用隔离build目录及Gradle项目缓存，没有clean共享输出、修改local.properties或停止其他进程。
- 当前机器复跑需先 `source /home/ko/android-tools/env.sh`；从android/YuyanIme执行Java GradleWrapperMain，使用 `/tmp/shurufa-rime-model-audit/display-build.gradle` 和 `.gradle/t9-display-audit`。命令与目标均保留在Gradle日志对应批次记录中。
