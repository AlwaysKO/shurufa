# 下午好第二轮：完整坐姿与无道具实验

2026-09-16，用户“继续”。沿项目 AGENTS.md、CREATION-POLICY.md、Vault记忆入口与GIF三层验收经验。旧稿、旧导出不覆盖，未改项目代码。

## 假设与本轮结果

上轮反复强调构图坐标未可靠解决留白，附参考编辑还改变角色和动作侧。本轮不再修改旧图：男性新做完整盘腿坐姿，雪纳瑞新做无道具固定坐姿。新增两个原始PNG，分别使用独立网页对话。

- 男性：双手完整、同一侧挥手连续，解决胸像裁手问题；但全身过大，鞋底贴近并部分进入输出字幕条。
- 雪纳瑞：取消道具后脚底跨行基线明显稳定，不再有旧版杯子颜色变化；但下方留白仍不足，脚底紧挨字幕。
- 两张均 needs-rework，不计入合格候选。本轮没有新增合格GIF，不用2/2机器通过掩盖布局问题。上一轮2个候选保持原状。

## 对话删除验证

先网页下载原图，用浏览器图片解码确认尺寸、Node SHA-256和本地Pillow复核，记录完整提示词与来源URL，然后删除专用对话。

男性删除后首页列表暂时仍有旧链接，流程主动暂停。只重新打开该已知本任务URL，页面随后自动返回首页、旧链接消失，确认删除后更新记录；没有删第二次或操作其他聊天。雪纳瑞下载、校验、删除、刷新复查均通过。原始PNG均保留本地。

## 验证与待讨论方案

仅调用现有 `renderReferenceCharacterGif` 与 `ORIGINAL_WEB01_ITEMS` 两个固定ID，在新目录生成诊断产物，未修改现有renderer、阈值或时间线，也未把新源图写入上轮masters。临时执行入口 `/tmp/render-web-completion.mts` 是本机诊断文件，不进入Git。

独立解码两诊断GIF：240×240、20帧、4000ms、loop0、小于250KiB；可见像素及alpha与首帧WebP一致。上轮4个GIF逐一哈希复核未变。真实GIF抽帧仅用于静态检查，没有用户动态批准，没有接入APK/API。

已向用户提出可选方案：允许生成后用程序对12个真实姿势采用统一尺度、固定基线与补白，不修改表情、不补画、不用单图缩放冒充动作；原图不变。此方案等待用户选择，本轮未执行，不自动升级为长期规则。母图、诊断GIF和预览只留本地；提交必要提示词、来源、质量记录。
