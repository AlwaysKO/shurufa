# 下午好：用户允许的局部构图整理

2026-09-16。用户对“程序统一缩小姿势、对齐脚底并补白，保留真实动作和原图”明确回复“可以”。本次只整理 `web-k005-a-clean-20260916` 与 `web-k005-d-seated-20260916` 的两份新稿；不是修改旧素材或对全词库自动处理的长期授权。

## 实现与来源

独立 `gridFraming.ts` 对严格4×3方格PNG采用同一缩放尺寸，不重新画图、不生成姿势、不逐格按包围框缩放或水平居中。原格整体缩放并保留于新白色画布，超界参数直接拒绝，不裁切来凑留白。所有源姿势仍来自网页真实绘制。

两原图为1448×1086，每格362；统一缩到290，实际比例290/362≈0.801105，目标脚底在格高69%附近。雪纳瑞12格参考基线均303，男性参考为308/309。参考值先按每行至少8个RGB最小通道<180像素测量，再结合已看过的完整源图确认固定脚底；该阈值是本次测量方法，不写成通用自动检测规则。

`scripts/render-web-framed.ts` 限定两个ID、源路径及SHA；来源对话清理未确认则拒绝，输出在独立目录并原子发布。`output/source-manifest.json` 保存完整提示词、网页来源、原始与新母图SHA和12格变换。重新执行需本地归档的原图，不是仅Git克隆即可运行。

## 验证

- 新增测试先因模块不存在失败；实现后2/2通过，覆盖同一尺度、基线、不同姿势保留、原Buffer不变、非法尺寸/参数/裁切拒绝。
- 三文件相关回归：44/44通过。
- 首次build发现脚本异步闭包中的results数组隐式any；补充明确类型后build通过。没有放宽TS或用any绕过。
- 两张现有renderer审计通过，独立Pillow验证240×240、20帧、4000ms、loop0、各低于250KiB。大小54964/60896字节；首帧WebP可见像素及alpha一致。
- 两张整理母图所有12格底部26%区域均无RGB最小通道低于245的像素；全格保留而非裁主体。
- 原图固定SHA检查通过，上两轮6个GIF哈希未变；原始母图文件也未写入。
- 本地真实GIF抽帧0/5/10/13/19检查：手掌、爪、鞋子完整，脚底与字幕间距稳定。独立只读代码与静态复核未发现重要问题；审查者独立运行2项构图测试通过。抽帧不是动态播放批准。

## 结果与边界

新增雪纳瑞、男性两个候选，与上轮海豹、考拉共四种风格。原图构图缺陷记录仍留在原批次，不把旧needs-rework文件改成已通过。当前 `preview.html` 链接正确的新旧四个候选。

缩小后男性面部细节变少、调色板颗粒仍可见，生动度、语义与循环需用户实际播放审核。全部staticCharacterReview/humanAnimationReview仍pending，publicationAllowed=false，不加入APK/API。

本轮未调用网页生图，无新增网页对话需要删除。母图/整理后母图/抽帧/预览只留本地；仅两个候选GIF、三个必要代码文件和计划/来源/验收文字本地提交，不push。保留其他并行开发改动。
