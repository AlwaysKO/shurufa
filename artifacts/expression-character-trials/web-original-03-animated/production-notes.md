# web03 制作与交付记录（2026-09-17）

来源：2026-09-17 当前用户指令及 CREATION-POLICY.md 最新默认入库规则。不是用户逐张观看证明。

- 首批目标：好久不见、欢迎、我来了，各写实动物、极简黑白、黏土/3D、原创中国成年男性，共12张。
- 每份母图使用独立对话，原PNG、完整提示词、来源、时间、SHA归档到 sources/<id>/source。先解码与归档，后删除并核实。旧聊天未删除。
- framing.json 是本批已合格源图的**累积清单**，运行渲染时不可漏掉已有项。按源图逐格显式脚底坐标，全格同一缩放比例；保留原图，没有逐格变形或裁肢。
- 运行：`cd server && npx tsx scripts/render-web03-framed.ts && npm run expression:import-keywords`。复用现有4秒20帧渲染器、原始审计阈值。逐帧/母图/回退图只留本地。
- 渲染器内部 trial-only 标记沿用旧渲染器隔离契约；正式接口可用状态由 approved-keyword-gifs.json 中用户默认授权单独赋予，不把内部标记当成用户观看证明。
- 第一张正常直接生成；第二张初次仅回复文字，补充直接生图指令；第三张初次无图，重新加载确认后选择网页“创建图片”；第四张初次要求参考图，明确本次从零原创、无参考附件后用“创建图片”生成。完整补充提示分别归档，不假称首次均成功。
- 界面中的“工作区有成员达到使用上限”横幅不等于本账号生图被阻止；已实际生成成功，没有充值或更换账号。
- 现有导入器只认批次 visual-review.json 的 needsRework，不能只在 output/source-manifest.json 标记废稿。此批后续如有失败项需明确写入 visual-review.json，不加入 framing.json。
- 首次定向测试38项通过；脚本来源门禁审查后补充负例，复现缺证据/失败状态可越过来源检查，再修复并通过。来源校验与素材视觉检查分开。
- 曾发现新增脚本 results 隐式 any 导致 scripts 构建失败，补充类型后重新 npm run build 通过。

最终数量、来源清理、接口和删除验收见 verification.json；实际后台播放与删除按钮证据见 ui-verification.json。

- line-arrived 首版母图完成归档与对话删除后，原始GIF审计 loopClosure 失败（0.02248263888888889 > 0.020355902777777775）；从未导入。已移入 sources/line-arrived/rejected/v1，保持原SHA与清理证据，status标明 rejected-technical-audit；准备新对话重新生成，不改变阈值、不修改失败母图。


## 本批最终结果
- 「好久不见」「欢迎」「我来了」各4种风格，共12张合格GIF实际入库；批准清单181→193，真实HONOR后台系统素材425→437（含既有静态图）、空组230→227。没有补录重复同义组。
- 13个独立生成对话均在下载、解码、记录来源/SHA后删除并核实，包括1个失败源的专用对话。cat-welcome同一对话的冗余第二份母图保留，不增加成品数。
- line-arrived v2通过原审计后入库，v1始终隔离且从未入库。没有裁肢、逐格变形、放宽阈值或修改旧图。
- 12张均240×240、12真实姿势编成20帧、4000ms、关键停留900ms、无限循环，43,287～81,288字节，原审计零问题。
- 真实后台三组各4张均可见、完成解码并具有删除按钮；可见前台两次截图证明各动图在播放。最初隐藏标签页不推进动画，激活后才记录播放证据，未把静态截图当播放验证。
- 12张逐张通过真实推荐、HTTP200下载、完整解码与SHA一致检查；隔离测试用户实际删除12张、重跑导入仍不推荐，真实用户不受影响；只清理自建测试记录。
- server构建通过，5文件43项定向测试通过；既有Fontconfig缓存版本警告未调整。独立只读审查未发现必须修复问题。
- 母图、逐帧、冗余/失败图及后台截图只留本地；Git可见范围仅最终GIF及必要代码、来源/提示词/验收文本。未强制添加、推送或打包APK；其他会话文件保留。
- 本批无未解决阻塞。后续仍有227个空语义组，不代表整库已补齐。
