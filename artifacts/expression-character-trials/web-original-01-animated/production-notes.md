# ChatGPT 网页首批：下午好

日期：2026-09-16。依据项目 AGENTS.md、完整 CREATION-POLICY.md、Vault 工作记忆入口和可靠自动化经验；用户允许修改提示词继续制作。当前分支，不建 worktree，不动旧 GIF 或其他开发改动。

## 本轮结果

- 本轮7个独立网页对话，8张母图：考拉v2；男性v1/v2/v3；雪纳瑞v1/v2；海豹网页自动提供两份。上一轮考拉首稿也纳入追溯，总计8对话9原图。
- 通过用户已登录的 Windows Chrome Dev 授权连接操作，只使用本任务新标签页。未读取cookie/密码，未删除旧聊天。
- 每份源图均从网页保存按钮下载，解码并记录完整提示词、SHA-256、来源URL后删除该专用对话；确认删除、回到首页并刷新检查目标链接消失。海豹网页意外双方案，先下载两张再删除同一对话。
- 海豹下载第一次超时的原因是“保存”展开了下载菜单，不是立即下载；选择“下载图片”后正常归档，未跳过校验或提前删除。
- 一次删除记录写入遇到临时交互命令共享base变量切换，网页删除已完成；随后独立检查旧链接不存在并修复本地记录。后续删除命令使用局部保存的路径与URL，逐步等结果，不跨图并发。

## 提示词实验与失败边界

- 考拉从自然语言改为逐格坐标、固定脚底、三行分别保留字幕安全区，改善了原先提前抬爪再收回的问题。源图每格底部26%区域在灰度阈值220下无角色内容，主体基线约259px稳定。这是本样片结果，不提升为全局可靠保证。
- 雪纳瑞首稿仍有跨行整体上移，杯子也有颜色变化。附参考编辑稿改了角色风格、杯子侧别，还发生前爪换侧，故不采用；保留首稿导出作为返修证据。
- 男性文字加强两版均未解决胸像过大和低位手掌被裁；附参考编辑第三版又产生明显行间位置漂移，有限尝试后停止。v2导出仅作问题追溯，不计候选。不通过放宽门禁凑齐四张。
- 海豹双方案中采用无网格线的第一张。轮廓/斑点轻微变化与动作节奏仍需用户播放复核；不是所有提示词坐标都精确执行。

## 代码与验证

仅在原固定清单、CLI与测试中追加 web01 四个下午好项，不改时间线、渲染算法、来源验证或发布门禁。先跑新增测试，因未知固定ID失败；最小实现后转绿。

- `npx vitest run src/expression/referenceCharacterRenderer.test.ts src/expression/prototypeAudit.test.ts`：42/42。
- `npm run build`：退出0。
- `npx tsx scripts/render-reference-characters.ts web01`：4/4机器审计通过。
- 独立解码四GIF：240×240、20帧、4000ms、无限循环、均小于250KiB；最大114871字节。可见像素与alpha和首帧WebP一致。
- 初次用Pillow直接比较全RGBA字节出现不一致；定位为完全透明像素中隐藏RGB的解码差别，可见像素及alpha无差异，修正验证口径后通过，未改图掩盖差异。
- 真实GIF的0/5/10/13/19帧生成本地接触表。独立只读视觉复核同样判定雪纳瑞、男性返修，海豹、考拉可以待用户播放审核。抽帧不等于连续播放认可。
- 独立代码审查未发现重要或阻断问题。Fontconfig旧缓存版本警告仍存在，不声称全仓库零警告。

## 交付状态

4个最终技术导出文件，只有海豹、考拉2个待审候选；雪纳瑞、男性2个needs-rework诊断导出不能计入可用数。全部静态/动态人工审核仍pending，publicationAllowed=false，不接入APK/API。

入口 `preview.html` 区分候选和待返修；`visual-review.json` 为质量状态，`output/report.json` 仅记录机器审计。标准CLI重建会覆盖通用output预览，应继续使用根预览和视觉状态，不据4个技术导出声称4图合格。

母图、抽帧、预览、Windows下载标记只留本地；仅海豹、考拉两个候选GIF及必要代码、提示词、来源与文字记录按规则提交；雪纳瑞、男性诊断GIF也只留本地，不push，不清历史。
