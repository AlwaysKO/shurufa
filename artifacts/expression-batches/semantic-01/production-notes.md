# semantic-01 生产记录

2026-09-09：用户确认按语义组开始执行后，先完成“理解知悉”组的“懂了”四风格基础成品。不是完整词库已完成，也未实现新匹配规则。

## 参考与原创

通过手机搜狗正常 UI 输入“懂了”，观察到“我悟了”漫画及人物反应类推荐。只学习“从疑惑到领会”的通用情绪机制；未将截图作为图生图输入，没有提取搜狗原始 GIF 或私有缓存。参考裁剪在忽略目录 `server/.runtime/expression-references/20260909-semantic-01/`，裁去聊天及广告列，许可未知，不发布。未点击推荐图/发送按钮；测试输入已清空，搜狗默认输入法与常亮关闭均已核验。

四张母版均使用内置 `image_gen.imagegen` 独立生成，完整提示及生成路径、SHA 记录于 `assets/expression/batches/semantic-01/manifest.json`。主体分别为原创饭团、小鸟、中国成年男性、黏土角色。每张四个不同关键姿势，确定性渲染完整汉字。root已检查母版与联系表，最终用户视觉验收仍 pending。

## 成品与验证

- 4/4 机器审计通过、0失败；全部240×240、16帧、1600ms、循环、低于250KB。
- `report.json` 包含逐图字节、SHA-256、逐帧信息；`preview.html` 可播放动画。
- 已复制到 `server/images/懂了/` 并同步根索引；状态均为 review-only。
- 服务端 `npm test`：335/335通过；`npm run build`：通过。存在 Fontconfig 版本缓存警告，未导致失败。
- 实现子代理完成TDD：新增6项先失败，最小扩展后针对测试56/56；独立规格与质量审查通过。
- 没有修改正式 catalog、API素材根目录或Android代码，没有构建/安装APK。

“明白”等同组词和完整句尾字幕变体尚未生成；已保留无字母版供后续复用。APK按用户要求积累后统一接入。
