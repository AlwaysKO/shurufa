# shurufa 项目工作记忆

## 多 App 发送与聊天采集（2026-09-17 用户明确要求）

- 微信、QQ、抖音按 App 和内容类型判断发送兼容性；修改前核对已验证实现和回归记录，不以一种交付方式无差别替换所有 App。选中 GIF 必须保留 GIF，AI 合成同样保持动画；不得以静态化或仅保存相册冒充发送成功。
- QQ、抖音聊天采集以微信现有完整能力为对齐基线，不得只实现通知采集。复用截图、会话分组、去重、持久化及上传补传链路，页面识别按各 App 实际结构适配；不得扩大到非聊天页面。
- 后台聊天采集按微信、QQ、抖音分别提供 Tab。真机测试由用户操作和发送，助手读取约定测试页面与诊断，不自动向联系人发消息。
- 来源：2026-09-17 本次用户连续要求。详细范围与待验证项见 `docs/plans/2026-09-17-multi-app-chat-parity-design.md`；此规则不代表功能已完成或真机验收通过。

### 统一采集原则补充（2026-09-17 用户明确修订）

- 所有App共享采集内容、触发、频率、去重、持久化和补传规则；App专属代码只处理页面/会话/内容区等适配差异，不允许只修一条入口遗漏其他入口。
- 首次进入聊天在页面可识别后立即请求截图，不得被固定2秒冷却挡住而漏掉短暂停留；重点防止同一会话内容未变化却反复采集。首次采集与最终重复内容入库应分开判断。
- 统一规则及现状差距见 `docs/plans/2026-09-17-unified-chat-capture-rules.md`；文档中的建议/未验收项不是已完成事实。
- 本轮排期恢复QQ完整聊天采集：先修好抖音，再接QQ，取代此前“QQ暂缓”的单次排期。
- 来源：2026-09-17 用户本次对2秒规则的纠正和恢复QQ采集要求。

## 推荐触发范围（2026-09-18 用户明确修订）

- QQ、抖音不自动展示或触发推荐图搜索；仅用户主动点击斗图按钮时搜索。不得通过关闭全局斗图开关误伤微信，微信继续沿用既有聊天输入框自动推荐规则。
- 手动搜索资格与实际图片/GIF发送能力分别判断，不把面板可用当作宿主发送成功；密码、搜索等非聊天输入仍不得放行。
- 来源：2026-09-18 用户本轮三项修复要求，第3项。此条取代QQ/抖音此前的自动推荐行为，不改变三App统一采集原则，也不代表已真机验收。

## 本机 WSL 真机连接（2026-09-18 用户提醒）

- 在本机 WSL 中调试手机时调用 Windows `adb.exe`，当前已验证路径为 `/mnt/c/Users/ES-11013/Android/platform-tools/adb.exe`；不能用 WSL 内独立 Linux ADB 的设备列表判断手机是否连接。
- 每次操作前用上述 Windows ADB 核对设备、实际安装版本与当前输入法，不因同一版本号推断已安装本会话包。此规则仅适用于本机 WSL 环境，不代表其他机器已有相同路径。
- 来源：2026-09-18 用户“记住你现在是wsl，你要通过windows连接”；不改变用户操作真机发送、助手只读取诊断的边界。

## GIF 与表情素材任务

- 涉及 GIF、动态表情、角色试稿或素材验收时，开始前完整读取 `assets/expression/CREATION-POLICY.md`。这是现有创作约束与后续用户更新的入口，不要只沿用旧计划中的参数。
- 再按本次关键词、角色或批次读取 `docs/plans/` 内相关计划与 `artifacts/` 内生产记录、报告及预览。只读取相关批次，不将全库素材或所有历史计划加入上下文。
- 区分已认可样片、新制作方向、静态认可、动态待审核和发布状态。机器质量报告或静态抽帧不能代替实际动态视觉验收；隔离试稿不等于可发布素材。
- 制作前简短列出沿用的用户要求、参考原文和本次验收方法；来源冲突先核对适用范围与后续修订，不能自行把某次试稿扩展为全局规则。
- 用户明确要求以后沿用的新规则，应更新现有 `assets/expression/CREATION-POLICY.md` 对应范围；批次专属反馈写回该批次记录。注明来源、日期及取代关系，保留用户已有修改与验收证据；不要再建立另一份参数总表。
- 本节只适用于素材相关任务，不给无关 Android、服务端或其他业务任务强加 GIF 创作条件。

## 跨项目记忆

- 本机 Vault 的 `04_系统维护/运行与验收/跨项目工作记忆入口.md` 提供检索与写回边界；Vault 不可用时继续使用上述项目原文并报告缺口，不假装读取成功。
- 本入口不授权修改旧素材、发布 APK/API、批准已有候选或清理用户未提交改动。AI 推断出的长期经验仍需审核。

## Git 体积与素材提交（2026-09-15 用户明确长期要求）

- 素材制作仅提交最终导出 GIF 和必要代码/文字/清单；母图、帧图、预览、废稿、视频等只保留本地，遵循 `.gitignore`，禁止用 `git add -f` 绕过。
- 细则见 `assets/expression/CREATION-POLICY.md` 的“Git 素材归档边界”；不能把此规则套到运行必需的图标、回退图、静态模板、字体或词库。
- 提交前检查 `git diff --cached --stat` 和 `git ls-files -ci --exclude-standard`；后者应无输出，发现已跟踪忽略文件先停止并整理。用 `git count-objects -vH` 观察仓库体积。
- 原图退出 Git 不等于删除本地文件。新克隆不含制作原图，相关重绘/创作验收需另拷本地归档。
- 本次历史重置是单次授权，未来不自动清历史或强推；保留其他未提交开发内容。

### 临时验证文件（2026-09-15 仓库整理）

- 2026-09-21 用户明确要求：真实聊天截图（`server/uploads/chat/`）只留本地，加入 Git 忽略；已跟踪截图仅从索引移除，不删除本地、不自动重写旧提交历史。截图不得混入后续代码提交；合成/脱敏的必要正式测试夹具与运行资源不受此目录规则影响。

- 临时诊断程序/数据、测试输出、覆盖率、浏览器报告及生成的 HTML 预览遵循 `.gitignore`，只留本地；诊断 Markdown 结论可以提交。
- 区分临时文件与可复现的正式回归测试。当前不笼统忽略 `src/test`、`*.test.ts` 或必要的小型测试夹具；本机专属、依赖仓库外环境脚本的检查不随仓库提交。
- 已被跟踪的忽略文件需仅从索引移出，保留本地实体；`.gitignore` 不会自动清除历史体积，不因此再次擅自重置历史。

## 发送配置与合成面板（2026-09-18 用户明确要求）

- 微信、QQ、抖音的图片/GIF发送规则按App、版本及内容格式由后台配置，兼容参数变更优先更新规则，不重新硬编码到APK；不把静态降级、保存相册、私有命令已交接当作GIF已成功发送。原图与AI合成走同一配置执行链。
- 用户已知并接受配置边界：只能调整客户端已内置的发送机制，全新协议仍可能需要升级APK。保持配置校验、最后有效缓存和回滚；后台配置是否已部署、具体App版本是否真机验证必须分别说明。
- 用户确认布局：推荐图紧凑一行；主动进入AI/Emoji合成时展开键盘区域，固定三个标签及明显返回键盘按钮，Emoji采用多行网格。频繁切换应保留同一查询和选择状态，不跨输入目标保留旧文字。
- 来源：2026-09-18 本会话“每个都做成配置，在后台能改”及后续“可以”；具体实现与验收见 `docs/plans/2026-09-18-delivery-config-panel.md`，不代表已上线或已真机验收。

## APK 固定交付目录与命名（2026-09-18 用户明确长期要求）

- 本项目每次开发完成并通过必要验证后，必须打包可独立安装的 APK，复制到 Windows `E:\Projects\shurufa-android\apk`（本机 WSL 为 `/mnt/e/Projects/shurufa-android/apk`），不能只放 `artifacts` 或 Gradle 输出目录让用户自行寻找。
- 文件名包含打包日期和 APK 真实版本号，统一 `shurufa-YYYY-MM-DD-v<versionName>-<versionCode>.apk`；同版本多次构建追加短 SHA256，避免覆盖或混淆。版本信息从 APK/构建元数据核对，不凭文件修改时间猜测。
- 复制后核对源文件和交付文件 SHA256、APK 签名，最终回复明确唯一推荐文件的 Windows 完整路径、版本、验证范围与未验收项。未通过必要验证的包不得标为已验收交付；Run 临时安装包不能代替可分享的 APK。
- 历史 APK 不擅自删除。其他机器不能访问该 Windows 目录时明确报告交付缺口，不声称已复制；APK仍遵循Git忽略规则，不随源码提交。
- 来源：2026-09-18 用户“每次开发好，打一个包放在 E:\Projects\shurufa-android\apk，用日期和版本号命名……以后其他会话打包都遵循”。适用范围仅 shurufa APK 交付，不授权自动安装、Git 提交或推送。

## 固定原签名与后续打包（2026-09-18 用户最终修订）

- 用户最终取消新密钥迁移，要求所有后续Android Studio打包沿用此前成功安装的旧证书；本条取代同日“使用新miaoyan密钥并轮换”的方案。
- 本机 E:\Projects\shurufa-android\miaoyan.jks 已替换为原 C:\Users\ES-11013\.android\debug.keystore 的完全相同副本；新建的不同证书已单独备份，不得再用于日常构建。固定别名androiddebugkey，密码仅保留本机私有配置，不进源码/记忆。
- 固定证书SHA256：a4626fa45c451154093333af3dbc3c6e1a3c7ba783eae399ea4786b5457b1287。由 tools/signing-delivery.gradle 对debug/release APK统一签名并校验，不生成/使用签名轮换链，缺配置或证书不符则拒绝交付。
- 普通assemble和IDE Run均使用原签名；Run的testOnly包不交付。分享包自动复制到既定E盘目录，文件名为 shurufa-YYYY-MM-DD-v<versionName>-<versionCode>-<debug或release>-<短SHA>.apk，以此补充前述命名规则。
- 不得卸载/清除数据解决签名冲突。相同签名不代表所有手机必定能装，仍需核对包名、版本、Android版本/ABI及系统限制；debug/release不共享应用数据。若设备已实际迁移到新证书，另行核对，不能强制回退旧签名。
- 来源：用户确认旧签名988f16db包安装成功，明确“把新建的密钥替换成之前的，不换签名密钥；以后Android Studio都用这个”。使用说明以 android/YuyanIme/tools/SIGNING.md 为准。此次未操作手机、未提交或推送Git。


## 个人词库的核心目标（2026-09-22 用户明确要求）

- 个人词库、后台添加和指定手机同步的目的，是让手机端更好地打字、更好地出现用户想要的候选词；不能只以后台新增成功、词条数量或同步提示作为目标已达成的证明。
- 本次已确认的功能方向：后台手动添加词语与拼音，再支持同步到指定手机；具体同步范围与主控/备份端处理须按当次设计核对，不把此目标当作自动合并所有手机词库、改主控或伪造学习次数的授权。
- 来源：2026-09-22 用户“手动添加词语和拼音，再支持同步到指定手机。记住我的目的，是为了让手机端更好的打字，也就是说更好地出现自己想要的候选词”。此记录是长期目标，不代表添加/下发功能已实现或手机候选已验收。


### 本地、线上与手机的增量并集（2026-09-22 用户明确修订）

- 本地和线上后台都可向选定手机增量添加词语，手机取已有词与各端新增词的去重并集；来源只有较少词或本次未选中的词，均不是删除手机词组的依据。
- 用户明确例子：本地20、线上100、手机200，两边都点同步也不能覆盖/减少手机已有词。重复同步不得重复制造词条或点击次数；验收仍以实际候选可用性为目标。
- 本要求取代旧“备份端不得下发任何词库内容”的范围，仅新增纯加法下发；同步不是批量删除、停用或自动合并所有设备的授权。设计和验证见 docs/plans/2026-09-22-dictionary-additive-sync.md，不代表已部署/手机已升级。
- 来源：2026-09-22 用户本轮“增量同步……不会删除手机端的词组，而是并集”。

## 公共关键词推荐图库（2026-09-23 用户明确要求）

- “关键词推荐图”的GIF/图片、对应关键词、匹配说法和排序为所有设备共享，不能再限定为某个设备专用。个人词库、聊天和合成素材不因此扩大共享范围。
- 本地后台修改须自动导出随Git提交的元数据清单和原图；main推送后的部署及新服务器迁移均须恢复，不能把仅提交图片当作关键词同步完成。
- 来源：用户以本地 `/stickers` 的“来砍我”上传为例明确要求。实现和同步边界见 `docs/SHARED_STICKER_SYNC.md`；手机接口验证与实际上线状态需分别报告。

## 推荐图发送后清空输入（2026-09-23 用户明确要求）

- 只有用户在发送确认界面点击“发送”后才清空对应输入框；点击“取消”、返回或关闭确认界面必须保留原文。
- 选择推荐图、准备素材、打开宿主确认框、接口仅接受文件交接，均不能视为用户确认发送。微信 `WechatSubmitted` 等交接结果不得直接触发清空。
- 清空只能针对本次发送关联的原编辑框和未被用户修改的原文，不能误删切换会话后的内容或新输入。
- 来源：2026-09-23 用户本轮“发送后，输入框没有被清空……只有点击发送才清空，取消发送不清空”。本条记录验收要求，不代表宿主确认回传已修复或真机验收通过。

## 聊天会话名与列表统计（2026-09-23 用户再次明确）

- 联系人／群名展示须去掉末尾表情、装饰和特殊符号，保留姓名中的正常中英文字及数字；截断省略号仍须保留其不完整语义。不能把图标误读成字母的问题仅靠删除真实尾字母处理。
- 用户提供的正确姓名及错误截图即为问题依据，优先使用已有只读线上访问自行定位，不要求用户再次证明识别错误。不得把已知姓名写入正式OCR纠错映射，也不得凭相似文本自动合并历史联系人。
- 聊天采集页会话总数须与当前手机、App及列表分组口径一致，不混用底层截图来源数量；删除会话／删空图片后同步刷新。
- 来源：本轮用户给出“煌家112Lucky王”真值、重申尾部表情/特殊符号不要，以及删除后仍显示705的反馈。验收记录见 `docs/plans/2026-09-23-chat-title-suffix-and-count.md`；不代表已部署或手机已升级。
