# 服务配置
PORT=3000
NODE_ENV=development

# PostgreSQL（WSL 本地）
PGHOST=localhost
PGPORT=5432
PGUSER=ime
PGPASSWORD=ime_dev_password
PGDATABASE=personal_ime

# 定时分析任务（cron 表达式，默认每 10 分钟）
ANALYZE_CRON=*/10 * * * *

# 个人使用固定 user id（后续可扩展多用户）
DEFAULT_USER_ID=00000000-0000-0000-0000-000000000001
